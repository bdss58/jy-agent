# Journal — 2026-05

Tier 3 / append-only / NEVER auto-loaded. Chronological session notes live here so they don't pollute MEMORY.md (the always-loaded index) or topic files (curated knowledge).

## 2026-05-01 00:08 [ship]
2026-04-30 — Shipped refactor/decompose-run-step (commit d7d8093) after resuming from an exception-quit.

Extracted step.run_step (~500 lines) into 9 `_<phase>` helpers (_prepare_step_batch, _compact_and_build_context, _build_step_options, _record_llm_usage_and_cost, _append_assistant_message, _execute_tool_round, _check_stuck_loop, _maybe_reflect, _maybe_checkpoint). Inline branches (verification gate, truncation retry, final StepContinue) kept per prior Codex guidance.

Resume workflow that worked:
1. `git status` + `git diff --stat HEAD` to see WIP scope (808-line step.py diff)
2. `grep -n "^def " jyagent/runtime/loop/step.py` to see the helpers already created
3. `pytest -x` → found 4 structural tests failing because they grep `step.run_step` for symbols that moved into helpers. Fixed each by pointing at the correct helper (inspect.getsource(step._maybe_reflect) etc.)
4. Full suite green (725 passed) → `codex review --base HEAD` before commit per the standing rule
5. Codex caught a P2: _execute_tool_round dropped `cancel_event=loop._cancel_event` from the execute_tools call, silently regressing the cooperative cancellation shipped in 7e388ec. No existing test caught it — unit tests build AgentLoop via __new__ with _cancel_event=None so the default arg masks the bug.
6. Fixed + added source-level regression test TestExecuteToolsReceivesCancelEvent, verified by `sed -i '' '/cancel_event=loop._cancel_event,/d'` that the test actually fires when the kwarg is removed.
7. 726 passed, 1 skipped → commit.

Key reusable lesson (distilled to a durable tip): When decomposing a large function, grep-based whitebox tests DO catch most wiring regressions, but ONLY if the tests actually name-check every load-bearing keyword-argument that was wired inline. The `cancel_event=` kwarg existed only in the call-site diff — no behavioural unit test pinned it end-to-end because AgentLoop.__new__ fixtures all set `_cancel_event = None`. Codex review caught it on the structural diff.

## 2026-05-01 00:39 [ship]
Shipped `fix(tools): bound run_shell output to prevent OOM kill` (commit 79a8e98).

Background: 2026-04-30 23:31 macOS jetsam SIGKILLed jy-agent Python (PID 83341, ~74 GiB resident). Codex correctly diagnosed: `subprocess.run(capture_output=True, text=True)` in `jyagent/tools/core.py::run_shell` buffered the entire child stdout+stderr in RAM before truncating to 50 000 chars. A runaway command could push the parent into multi-GB territory.

Fix design (mirrors the existing background path's discipline):
- New `_BoundedStreamReader(threading.Thread)` drains a binary pipe into bounded head (8 KB) + tail (128 KB) bytearrays.
- New `_drain_proc_bounded(proc)` wires up two readers (stdout, stderr) with `on_overflow=SIGKILL pgroup` at 32 MB per stream.
- Unified the legacy and cooperative-cancel paths into a single `Popen + drain + poll` flow. `_cancel_event` is now just a poll-loop predicate.
- `text=False` (bytes) — decode at `.collect()` time, cuts memory ~2× vs the str-decode-everything path.
- Defensive: keep draining after overflow fires so child's `write()` doesn't block on a full pipe; SIGKILL still works.

Bug found during testing: 50 000-char user-visible cap was applied AFTER appending the overflow marker → marker got chopped off when output filled the cap. Fixed by reordering: cap stdout/stderr first, then append overflow marker (always visible).

Tests: 11 new in `tests/test_run_shell_bounded.py` — RSS-bound (500 MB stdout → parent grows <100 MB), overflow tagging, stderr-only overflow, cancel still works, helper unit tests. Full suite: 734 passed.

Files: `jyagent/tools/core.py` (+208 / -57), `tests/test_run_shell_bounded.py` (new, 225 lines).

## 2026-05-01 01:26 [debug]
Found and fixed a head/tail boundary bug in `_BoundedStreamReader.collect()` (commit f5a5b2c) while auditing what the LLM actually sees from bounded tool results.

Pre-fix logic:
    truncated = self._total > (len(self._head) + len(self._tail))
    if not truncated: return bytes(self._tail)

The "not truncated" branch is correct only when `total <= tail_max`. When `tail_max < total <= head_max + tail_max`, the tail buffer has already dropped its early bytes (head still has them), but the code returned tail-only — silently losing the head bytes.

Default config (head=8 KB, tail=128 KB) → buggy band (128 KB, 136 KB]. After the 50 K char cap, the LLM saw an arbitrary middle slice (~[total-128K, total-78K]) instead of the start.

Fix: three explicit regimes:
  1. total ≤ tail_max         → return tail
  2. total ≤ head_max+tail_max → head[:total-tail_max] + tail (no marker, no loss)
  3. otherwise                 → head + elision marker + tail

5 new tests, all 3 regime-2 tests fail against pre-fix (verified by stashing). Full suite 742 passed.

Pattern lesson: when implementing head+tail bounded buffers, the "is the data complete" condition is `total <= tail_max`, NOT `total <= head + tail`. The latter is when there's no GAP, which is different.

## 2026-05-01 01:47 [ship]
Shipped commit 075b943: spill-to-disk + env-configurable caps for run_shell.

Closes the partial-context gap with Claude Code by making large-output recovery possible, while keeping our head+tail inline preview (better than Claude Code's head-only ~2 KB preview, per issue #17944).

Design highlights:
1. **Lazy spill activation** at exactly the right moment: when next chunk would push total > tail_max. At that instant, tail buffer still holds bytes [0, total) — dump it to /tmp, then tee every subsequent chunk. Memory cost stays O(head + tail), no extra pre-spill buffer needed.
2. **Spill survives SIGKILL**: when overflow kills the child at 32 MB, spill keeps writing — file captures ~all pre-kill bytes.
3. **Factory pattern** for spill path: tests inject a mock factory; production factory creates `/tmp/jyagent_runshell_<out|err>_*.out` via mkstemp. Factory raising = silent fallback (read-only rootfs etc).
4. **Env-var config** with explicit stderr warnings on malformed values — defends against Claude Code's #17944 regression where BASH_MAX_OUTPUT_LENGTH was silently ignored after v2.1.2. Knobs: HEAD_BYTES, TAIL_BYTES, HARD_KILL_BYTES, OUTPUT_CHAR_CAP, SPILL (bool), SPILL_DIR.

12 new tests, all 28 pass. Full suite: 754 passed, 1 skipped.

End-to-end demo confirmed: 200 KB stdout → inline output capped at 50 K chars + truncation marker + spill marker pointing to /tmp file containing all 200 000 bytes. Agent can read_file the spill to recover.

LLM-visible behavior comparison vs Claude Code:
- Inline preview shape: us = head + elision + tail (preserves end of structured JSON); Claude Code = head-only ~2 KB (loses end).
- Recovery: us = read_file on spill path; Claude Code = Read tool on tool-results file.
- Cap configurability: us = env var with loud warnings on misconfig; Claude Code = env var that silently breaks across versions (#17944).

## 2026-05-01 08:11 [correction]
2026-05-01: User caught me using "2025" in web_search queries during an agent-context-management research task, while the same tool batch included `date` confirming 2026-05-01. Root cause: queries composed before date result was read (parallel batch). Added gotcha rule: run `date` in an EARLIER batch, not parallel, for time-sensitive research. The existing "verify current date" rule didn't prevent this because it didn't specify ordering within parallel calls.

## 2026-05-01 08:33 [ship]
Context-management self-review + 3 fixes shipped (branch ctx-mgmt-fixes, commit d73ae41).

Research phase:
  - Reviewed Anthropic "Effective context engineering for AI agents" (Sep 2025),
    LangChain Deep Agents context offloading, JetBrains Dec 2025 efficiency study.
  - Consensus stack: attention budget / compaction / tool-result clearing /
    structured note-taking / progressive disclosure / sub-agents /
    prompt-cache stability / hybrid summarize+retrieve.

Self-review via codex exec (read-only):
  - codex --model gpt-5-codex failed (ChatGPT plan doesn't support gpt-5-codex).
    Retry with default model worked. Stuck on web search retries 1/20 —
    re-dispatched with "DO NOT use web search" which completed cleanly.
  - Codex confirmed strong architecture: two-tier compaction, signed-thinking
    preservation, three-tier memory with hard caps, cache-friendly compaction,
    post-compaction file re-injection, progressive skill disclosure,
    sub-agent isolation. Flagged weaknesses that became the three fixes.

Fixes implemented:
  1. memory/compaction.py _choose_split_point() walks keep_recent boundary
     left past orphan tool_results → prevents post-/compact provider validation
     failures.
  2. runtime/loop/compaction.py _dehydration_placeholder() preserves
     jyagent_runshell / jyagent_bg spill-file paths in cleared placeholders →
     rehydratable observations.
  3. skills.py pre_route_for_turn() + agent.py wiring → SKILL_PRE_ROUTER
     actually runs when enabled (was dead code).

Tests: 13 new tests (tests/test_context_mgmt_fixes.py), all pass.
Full suite: 764 passed, 4 skipped (was 751).

Remaining open items (ranked in the review, not implemented this session):
  #4 Tighten sub-agent memory injection scope (add inherit_memory flag, share
     skill catalog) — scope: medium, user privacy win.
  #5 Fix pricing coverage for claude-sonnet-4-6 default + sub-agent API-call
     accounting (currently 1 per whole sub-agent regardless of steps).
  Bonus: stabilize _cached_memory_context (currently nulled every turn,
         contradicts its own comment), move from 4-chars/token heuristic to
         real provider count_tokens, hybrid summary+retrieval for /compact
         prefix.

Workflow lessons:
  - git worktree + uv sync in worktree worked as expected (editable install
    pointed at worktree, not main).
  - `codex review` doesn't accept --sandbox or --skip-git-repo-check; use
    `codex exec --sandbox read-only` instead for this pattern, or just
    `codex review <prompt>`.
  - Codex web-search retries 1/20 hang-loop is a known failure mode; always
    include "DO NOT use web search" in the prompt when my own web search
    already covered the external sources.

## 2026-05-01 08:50 [ship]
Shipped remaining context-management fixes #4 + #5 on branch ctx-mgmt-fixes, commit 65057d0.

Fix #4 — Sub-agent memory scope gate
  - New `memory_mode` schema param on dispatch_agent: "none" (default),
    "matched" (BM25 retrieval), "full" (legacy 4KB dump).
  - DEFAULT FLIPPED from "full" → "none". Sub-agents no longer auto-inherit
    MEMORY.md. Callers must opt in.
  - _get_memory_context(query, mode) drives BM25 via search_memory() for
    matched mode.

Fix #5 — Pricing coverage + sub-agent accounting
  - _MODEL_PRICING now covers claude-sonnet-4-6/4-5/4, claude-opus-4-5/6/7.
    Verified prices against platform.claude.com/docs/en/about-claude/pricing
    (2026-05-01): Sonnet $3/$15, cache-write $3.75 (1.25× input),
    cache-read $0.30 (0.1× input).
  - LoopResult gained total_cache_creation_tokens / total_cache_read_tokens
    / api_calls (default 0).
  - RunState accumulates all three per step; 10 _finalize_run() call sites
    patched in engine.py + step.py.
  - _make_subagent_outcome + record_subagent_usage plumb them through.
    Parent stats now sees the child'"'"'s REAL api_calls and cache activity
    instead of the legacy "+1, +0 cache" roll-up.

Workflow notes:
  - Mass edit across 10 _finalize_run sites done via Python regex script
    in a single run_shell — assertion-guarded count verification prevents
    partial-replacement bugs. Safer than sed for multi-line inserts.
  - Verifying Anthropic pricing via web_fetch BEFORE commit caught a bug:
    initial table included invented "claude-sonnet-4-7" and "claude-haiku-4-6"
    entries; official page has neither (only 4.6/4.5/4). Fixed before
    commit. Honesty principle > speed.
  - Test-source-inspection assertions (test_loop_engine_critical.py'"'"'s
    4000-char fallback-block window) are fragile proxies — a refactor that
    preserves intent can still break them. Fix was to widen bound, not
    change structure.
  - Regression in test_subagent.py: test defined blocking_subagent with
    FIXED positional signature; adding _run_subagent kwarg broke it. Tests
    that shadow internal functions should use *args/**kwargs.

Cumulative branch state: 2 commits, 777 passed / 4 skipped (was 751 baseline).
Ready for main merge:
  cd ~/jy-agent && git merge ctx-mgmt-fixes && git worktree remove ../jy-agent-ctx

## 2026-05-01 08:56 [ship]
Merged ctx-mgmt-fixes into main (commits 585c6c5 + ff09060, rebased from d73ae41/65057d0 onto main which had moved forward with retry_attempts bump).

- Rebase was clean (no conflicts) — my changes touched different regions than the retry bump.
- `git merge --ff-only` succeeded after rebase.
- Full suite on main: 780 passed, 1 skipped. (Delta from worktree's 777/4 is test skip-count drift from env-dependent tests; no regressions.)
- Worktree ../jy-agent-ctx removed, branch deleted.

Unstaged data files on main (MEMORY.md, journal/2026-05.md, web-search SKILL.md) preserved untouched throughout the merge — they sit in data/ & skills/ paths which the branch never modified.

## 2026-05-01 09:28 [ship]
## 2026-05-01 — Removed SKILL_PRE_ROUTER per-turn auto-router

**Decision:** User asked "is SKILL_PRE_ROUTER proper to remove?" — I analyzed
and recommended removal. User said "go ahead remove". Shipped as commit
f046454 on main (-139 LOC, 762/762 tests pass).

**Rationale:**
- Opt-in (default OFF), never enabled in any session trace.
- Added latency+cost per turn when ON; carried test/doc/refactor weight
  when OFF — the speculative-infrastructure anti-pattern.
- Main-model self-activation via manage_skills (progressive disclosure,
  same pattern as Claude Code) works fine for the current ~7-skill
  catalog; router would only pay off at ~50+ skills.

**Scope correction mid-task:** Initial plan was to delete the entire
routing mechanism (_route_llm, _route_keywords, auto_activate_for_query).
Caught that `skills/create-skill/scripts/test_trigger.py` uses
`auto_activate_for_query()` as its eval-driven-development API —
blanket removal would have silently broken the create-skill workflow.
Narrowed scope to remove only the **per-turn** wiring; kept the routing
methods as an explicit eval-only API.

**Removed surface:**
- `SKILL_PRE_ROUTER` env flag + `SKILL_PRE_ROUTER_ENABLED` config
- `SkillManager._auto_activate`, `pre_router_enabled`, `pre_route_for_turn()`
- Call site in `jyagent/agent.py` main loop
- `TestSkillPreRouterWiring`, `TestPreRouterEnvVar`
- Doc references (system prompt, `.env.example`)

**Retained (retargeted as eval API):**
- `SkillManager.auto_activate_for_query()` + `_route_llm` / `_route_keywords`
- `SKILL_ROUTER_PROVIDER/MODEL/TIMEOUT` env vars (now eval-only knobs)
- `tests/test_skill_router.py`

**Process notes:**
- Used `git worktree` + local `uv sync` per the worktree-editable-install
  gotcha — verified `.venv/bin/python -c "import jyagent"` resolves to the
  worktree's source before trusting tests.
- Tried `codex review` and `codex exec --sandbox read-only` as second
  opinion per workflow rule — both timed out at 300–600s with empty output
  (recurring Codex CLI issue, not related to this change). Self-verified
  via full pytest (762 passed), import smoke test confirming removed
  surface is gone and eval API retained, and grep sweep for dangling refs.
- Commit f046454 on main. Branch deleted, worktree removed.

**Memory updates:** Remove two stale MEMORY.md tip/correction lines about
SKILL_PRE_ROUTER=0 default behavior (done separately).

## 2026-05-01 09:40 [ship]
## 2026-05-01 (later) — Deleted the whole skill routing mechanism

Follow-up to the SKILL_PRE_ROUTER removal earlier today.  User reviewed my
partial removal (kept auto_activate_for_query as eval-only API) and said:
"delete the whole routing mechanism and amend test_trigger.py".  They were
right — keeping ~300 LOC of router code in jyagent/skills.py for a single
downstream eval script was worse than inlining a small helper in the script
itself.  Shipped as commit b1be8a8 on main.

**Design shift:**
- Before: `SkillManager.auto_activate_for_query` + `_route_llm` +
  `_route_keywords` + catalog mutation via `deactivate_all()` → used by
  `scripts/test_trigger.py`.
- After: `scripts/test_trigger.py` owns a self-contained ~50-LOC
  `_route_query(catalog, query, runtime_owner)` helper.  `SkillManager`
  exposes only `get_catalog()` (data) and `list_skills()` (validation).

**Why inline was right:**
- Eval tooling is the only caller — zero reuse loss.
- No more SkillManager state mutation during eval (no `deactivate_all()`
  dance); each query is a pure function call.
- `jyagent/skills.py`: −253 LOC; eliminated `json`, `time` imports.
- Removed `SKILL_ROUTER_PROVIDER/MODEL/TIMEOUT` + `get_skill_router_model_spec`
  from config.  Renamed `SKILL_ROUTER_MODEL` → `SKILL_IMPROVE_MODEL` in
  `improve_description.py` (it's a description rewriter, not a router —
  the old name was a vestigial shortcut).
- Deleted `tests/test_skill_router.py` entirely (335 LOC).
- New eval-local env vars: `SKILL_TRIGGER_EVAL_MODEL` /
  `SKILL_TRIGGER_EVAL_PROVIDER` (default: active agent runtime).

**Net across both passes today:**
- f046454 (pass 1): −139 LOC
- b1be8a8 (pass 2): −505 LOC
- Total: **−644 LOC**, ~8 env vars → 3, 2 test files consolidated.

**Verification:**
- Full pytest suite: 751 passed, 4 skipped (pre-existing).  −11 from
  `main` matches the deleted `test_skill_router.py` file.
- Live-LLM end-to-end test: ran rewritten `test_trigger.py` against
  `skills/web-search` with a toy eval set — correctly routed a
  "search web for latest Python" query to web-search skill.
- Surface assertion: `SkillManager.auto_activate_for_query` et al gone,
  core skill-management surface (`activate`, `get_catalog`, body blocks)
  intact.

**Lesson:** My initial "compromise" to preserve the eval API as a public
method on SkillManager was a false economy.  Inlining a 50-LOC helper
in the single caller is cleaner than exposing 300 LOC of public API.
When a refactor has exactly one downstream consumer, the shape of the
refactored module should not be warped by that consumer's ergonomics
— put the shape *in* the consumer.

## 2026-05-05 21:48 [refactor]
UI package refactor — six self-review fixes shipped on `ui/refactor-2026-05` (worktree at /Users/jyxc-dz-0100398/jy-agent-ui-refactor, commit 015c89e). After analyzing jyagent/ui/ design vs. 2026 CLI agent landscape (Claude Code, Codex, Aider, Crush et al.):

1. render_final_text() moved from agent.py → ui/terminal.py (UI owns display).
2. StreamingUI dataclass replaces (callbacks,spinner) tuple + callbacks._stream_state side-channel; flush_trailing_newline() is now a real API.
3. tools/display.py — per-tool icon + arg formatter extracted; ui/terminal.py no longer switches on tool names.
4. ui/commands.py — Command registry; single source of truth for /help and dispatcher (kills help-drift bug).
5. on_tool_pre_execute LoopCallbacks hook + --ask flag (JYAGENT_ASK env); returns "deny" → engine synthesizes denial ToolResult in place of real call, keeps on_tool_start/end pairs balanced. Auto-allows non-mutating tools via existing _TOOL_METADATA. "a" answer = allow-all session.
6. Renderer Protocol + TerminalRenderer extraction. CLI(TerminalRenderer) now ~120 lines (was 304); seam unblocks Textual/Web/recording front-ends without agent.py changes.

Tests: +11 new, 764 passed / 4 skipped / 0 failed. Existing test_step_runner.py FakeLoop needed _fire_with_return added (would have broken otherwise — gotcha worth remembering: any new helper on LoopThreadHelper needs mirroring on test fakes that don't inherit it).

Branch not merged to main. To merge: `cd jy-agent && git merge ui/refactor-2026-05` (or PR).

## 2026-05-05 22:38 [codex_review]
Codex review of UI refactor (commit 015c89e) — first run on neo provider hung in reconnect loop (12/20 retries in 8.5min, killed). Second run with `-c model_provider=models-proxy` did real analysis but timed out at 25min deadline before producing final summary. Reasoning trail in /tmp/jyagent_bg_os0e_q9z.out (8000 lines) had 3 substantive observations:

1. **CRITICAL edge-case bug** — denied tool calls bypassed _check_stuck_loop because _execute_tool_round returned empty tuples when all were denied. Model could infinite-loop on repeated denials. Fixed by including denial ToolResults in returned tuples.

2. **SHOULD-FIX ordering bug** — mixed batch (some denied, some allowed) had tool_result messages out of original tool_call order. Refactored _execute_tool_round into split → dispatch → ordered-emission phases.

3. **Documentation gap** — Renderer protocol scope. Clarified in renderer.py docstring that input concerns are intentionally separate.

Both bugs were genuine and would never have been caught by my self-review or the test suite as written. Codex earned its keep even without producing a final summary — the mid-stream reasoning was specific enough to act on.

Committed as b9c390b. Full suite: 768 passed, 1 skipped.

Lesson: don't blame codex when the final summary is missing — the reasoning IS the review. Grep the output file for the analysis even if the prompt's requested format never landed.

## 2026-05-06 00:16 [refactor]
Acted on Codex review of jy-agent runtime (recommendation #1): break step.py ↔ engine.py coupling.

Branch: refactor/run-context-extraction (off main).

Phase 1 — commit 42977bb:
- New file jyagent/runtime/loop/finalize.py: hosts finalize_run, is_truncated, strip_dangling_verification (was: _finalize_run, _is_truncated, _strip_dangling_verification in engine.py).
- engine.py keeps the underscore names as aliases (`from .finalize import finalize_run as _finalize_run, ...`) for back-compat with tests/out-of-tree imports.
- step.py's 3 `from .engine import _finalize_run[, _is_truncated]` lines now import from .finalize.
- Pure relocation. 184 loop tests pass.

Phase 2 — commit e1f32b3:
- New file jyagent/runtime/loop/run_context.py: defines RunContext as a `@runtime_checkable Protocol` describing exactly the surface step.py uses on its driver: 7 read attrs (_config, _callbacks, _runtime_owner, _model_spec, _tool_source, _executor, _cancel_event), 3 mutable cross-turn attrs (_partial_side_effects, _run_id, _todos), 5 methods (_fire, _fire_with_return, _is_cancelled, _call_llm_with_retry, _write_checkpoint).
- step.py: `from .engine import AgentLoop` (TYPE_CHECKING) → `from .run_context import RunContext`. All 11 `loop: "AgentLoop"` annotations → `loop: "RunContext"`.
- AgentLoop satisfies the Protocol structurally; no inheritance change. Type-only refactor.
- 768 tests pass full suite (1 skipped, 0 failures).

Caveat encountered: pre-existing uncommitted edits in jyagent/memory/extraction.py + operations.py (autonomous memory consolidation work in flight in working tree) were causing test_memory_phase1.py::test_extract_topic_description_long_line to fail. Confirmed via `git stash push -- jyagent/memory/...` that the failure is unrelated to this refactor. Stashed during commit, popped after.

Result: step.py now has a clean explicit contract for what it requires from its driver. The other Codex recommendations (mutating-tool subprocess isolation, typed message models at provider boundaries) remain open.

## 2026-05-06 00:27 [ship]
Shipped Codex-review fixes #1-#5 for the memory subsystem (CRITICAL UPDATE-delete bug + 4 quick-wins).

Files changed:
- jyagent/memory/operations.py: moved _PROTECTED_SECTION_HEADERS here, added _compute_protected_indices(), changed forget_from_memory_md signature to (count, preview, protected_skipped) with min_keyword_len=6 + protect_sections=True defaults; rewrote _extract_topic_description through new _sanitize_topic_description (cap=120 chars, strip control chars/markdown); write_topic now uses tools.core.atomic_write; public forget() returns preview + protected count.
- jyagent/memory/extraction.py: imports _PROTECTED_SECTION_HEADERS / _compute_protected_indices from operations; _replace_line now does in-memory delete of matched_indices instead of calling forget_from_memory_md (closes the CRITICAL: shared-keyword across protected/unprotected lines no longer wipes protected lines).
- jyagent/tools/facades.py: search action now defaults journal_months=6; `category="all"` opts into full-history search.
- tests/test_memory_upgrades.py: +13 hardening tests (CRITICAL repro, forget min-keyword, forget protection, forget preview, internal min_keyword_len=0 bypass, topic-desc length cap, control-char strip, whitespace collapse, journal_months default, all-keyword opt-out, atomic write under simulated mid-write).
- tests/test_memory_phase1.py: bumped topic-desc length-cap test from 80 → 120 chars.

Tests: 110 memory tests pass per-file in alphabetical order; 777 total tests pass across the full suite (1 skipped, 0 failures).

Remaining open items from Codex review (not shipped this round):
- HIGH: prompt-injection through auto-extraction ADD (extraction.py:72-80) — needs intent filter on directive bodies.
- MEDIUM: build_memory_context truncates before topic listing (context.py:25-34).
- LOW: journal category sanitisation in append_journal.
- Larger refactors deferred: stable memory IDs (item 7), project-scoped memory (item 8).

## 2026-05-06 00:45 [refactor]
RunContext extraction — Phase 3 (latent-bug fix from Codex review).

After Phase 2 commit, ran `codex exec --sandbox read-only` to review the two refactor commits (42977bb, e1f32b3). Codex found:

1. **Real latent TypeError**: step.py::_maybe_checkpoint passes total_cache_creation_tokens / total_cache_read_tokens / api_calls to loop._write_checkpoint, but AgentLoop._write_checkpoint signature didn't accept them. Hidden because:
   - default checkpoint_every_n_steps=0 disables the path
   - test_step_runner mocks _write_checkpoint(self, **kwargs)

2. **Minor**: trace-finalize warnings now log under `jyagent.runtime.loop.finalize` instead of `jyagent.runtime.loop.engine` (different `__name__` per file). Acceptable.

Commit 16c11f7 fixes #1:
- LoopCheckpoint: add total_cache_creation_tokens, total_cache_read_tokens, api_calls fields (default 0; from_json filters unknown keys so old checkpoints load).
- AgentLoop._write_checkpoint: accept all three kwargs (kw-only, default 0).
- RunContext._write_checkpoint: Protocol signature extended to match.
- tests/test_checkpoint.py::test_accepts_cache_and_api_call_kwargs: regression test, asserts round-trip to JSON.

Final state on branch refactor/run-context-extraction:
- 4 commits (3 runtime refactor + 1 unrelated autonomous-memory commit b346b1c that landed during the session)
- 778 tests pass / 1 skipped / 0 failures (was 768; +10 from regression tests)
- Codex recommendation #1 (RunContext extraction) DONE.
- Open from Codex review: #2 mutating-tool subprocess isolation (hard); #3 typed message models at provider boundaries (medium).

Lesson: writing down a Protocol surface forced the latent call-site contract bug to surface. Worth doing for any module pair where the boundary is "implicit duck-type". Same pattern that exposed contracts in LLMClient earlier.

## 2026-05-06 09:02 [refactor]
Shipped Codex recommendation #3 — typed messages + provider-boundary validation.

Branch: refactor/typed-messages (4 commits).

Phase 3a (a96916c) — Validators:
- New jyagent/llm/validation.py (357 LOC): pure functions narrowing Any -> TypedDict for UserMessage / AssistantMessage / ToolResultMessage / Message / Context / StreamEvent.
- MessageValidationError(TypeError) carries a path-pointer like 'messages[3].content[1].arguments' for precise adapter-drift diagnosis.
- Strict on required, lenient on optional (forward-compat); unknown stream event TYPES rejected because the stream loop dispatches on type explicitly.
- 40 test cases including real Anthropic / OpenAI Responses adapter shapes.

Phase 3b (6a2bcbc) — Tighten LLMClient Protocol:
- complete: context: dict -> Context; return dict -> AssistantMessage.
- stream: context: dict -> Context; return Iterator[Any] -> Iterator[StreamEvent].
- TYPE_CHECKING-guarded import keeps the Protocol module load cheap.

Phase 3c (f6a066a) — Provider-boundary validation:
- LoopConfig.validate_provider_output: bool = False (default off; defense-in-depth).
- JYAGENT_VALIDATE_PROVIDER_OUTPUT=1|true|yes|on env var override.
- LLMRunner._should_validate_provider_output() resolves on every call (microseconds; no caching).
- Wired into call_complete (after final_message assigned) and call_streaming (terminal event consumed).
- Streaming path now tracks final_message_source: 'stream:done' | 'stream:error' | 'stream:fallback' so the validator names the exact boundary on failure.
- 14 new tests in tests/test_llm_runner_validation.py.

Phase 3d (3577f9a) — Tighten 6 boundary signatures:
- LoopResult.messages, AgentLoop.run, AgentLoop._run_impl, finalize_run, strip_dangling_verification, compact_messages -> list[Message].
- Deep internal helpers stay bare list[]; callers infer.

Real bug caught by validator (during 3c testing):
- tests/test_llm_client_protocol.py::_FakeOneShotClient used 'stop_reason': 'end_turn' (Anthropic wire-format) instead of 'stop' (runtime-normalized). Validator caught it; pytest hung in the 10-retry loop on first JYAGENT_VALIDATE_PROVIDER_OUTPUT=1 pass. Fixed + annotated.

Final test posture:
- 846 passed, 1 skipped, 0 failures with JYAGENT_VALIDATE_PROVIDER_OUTPUT=1
- Same with the env unset (proves zero-overhead opt-in).

Codex recommendations status:
- #1 RunContext extraction — DONE (previous branch refactor/run-context-extraction)
- #2 Mutating-tool subprocess isolation — open (large; needs design doc)
- #3 Typed message models + boundary validation — DONE (this branch)

## 2026-05-06 11:39 [reflection]
Session reflection: memory hardening + role scoping.

Shipped 3 commits closing all 8 Codex-review items on jyagent/memory/:
- b346b1c: CRIT _replace_line + 4 quick wins (forget hardening, topic-desc sanitisation, atomic topic writes, journal_months default)
- b21ad6c: HIGH prompt-injection defense on auto-extraction ADD (prompt-level SECURITY paragraph + structural _looks_like_injection filter)
- 198fb17: MEDIUM build_memory_context cap-bypass + dedup + LOW journal category sanitisation

+25 tests. 846 total passing. 0 regressions.

The more important outcome was the role-scoping conversation at the end. I had queued "project-scoped memory" and "stable memory IDs" as natural next items based on field convergence (Claude Code, Cursor, AGENTS.md, Mem0, A-MEM). User asked whether those fit my role.

Honest answer I landed on: NO for project-scoped memory (those tools are IDE-bound coding agents; user launches me from ~, most work spans multiple repos, Tier 2 topics already handle per-project detail). OPEN-BUT-DON'T-SHIP for stable IDs (fixes tomorrow's problem; costs 2 KB of Tier 1 prompt; CRIT already fixed at root).

User chose "nothing" — stop-and-stabilize. Correct call.

Lesson for myself: "field convergence" is a valid signal but not a directive. Always check whether the convergent pattern applies to MY role (personal assistant from ~) vs. the reference system's role (IDE agent in a repo). I was pattern-matching without role-checking. The Codex review was in-scope; the "next" refactors were out-of-scope for what I actually am. Queue-completion instinct is a bias; push back on it.

## 2026-05-06 12:35 [session]
Researched Anthropic's "session" concept in Managed Agents and reviewed jy-agent's own implementation, then fixed the highest-ROI gap.

**Research sources:**
- https://www.anthropic.com/engineering/managed-agents (Martin/Cemaj/Cohen, Apr 2026) — defines session as the append-only log; core thesis is three-way virtualization: brain (harness) / hands (sandbox, execute(name,input)→string) / session (event log). API surface: emitEvent, getEvents (positional slice), getSession, wake(sid), provision({res}). Session is the only pet; brain and hands are cattle. TTFT p50 dropped 60% / p95 >90% after decoupling.
- https://code.claude.com/docs/en/agent-sdk/sessions — thinner SDK-level notion: conversation + tool calls + results, disk-persisted, operations: continue / resume(id) / fork(id), session_id from ResultMessage, SessionStore adapter for external storage.

**Review of jy-agent/jyagent/memory/session.py:**
Two real bugs found:
1. **Save-on-exit is crash-lossy** — save_session runs only in _graceful_exit. SIGKILL / OOM / power loss → entire current-session conversation lost.
2. **ConversationMemory is both log AND view** — compaction replaces messages in place, next save overwrites latest.json with post-compaction state, violating the "append-only log + transformed view" separation that Managed Agents treats as load-bearing.

Smaller gaps: no fork(), file-state untracked (same as SDK), archive pruning silent at keep=20.

**Fix shipped (bug #1 only):**
- New checkpoint_session() in memory/session.py — atomic write of latest.json ONLY, no archive, no prune. Safe to call per-turn.
- New _checkpoint_turn() helper in agent.py that wraps it with stats metadata and swallows exceptions.
- Called at two points in agent.py run loop: after conversation.add_message("user", ...) and after planner_messages extend/add. User input now durable before engine runs; assistant turn durable after completion.
- atexit.register(_checkpoint_turn, conversation) in run() as belt-and-braces.
- 6 new tests in tests/test_session_checkpoint.py — lock in: no-archive invariant, idempotence, empty no-op, roundtrip, save_session still archives (regression), agent helper safe on None.

Result: 130 tests pass (124 existing + 6 new). Diff: +68/-2 across 3 files (+1 journal). Bug #2 (log/view conflation) left for later — more invasive, would need a JSONL event log.

## 2026-05-06 13:03 [session]
Bug #2 fix: log/view conflation in jy-agent session. Co-worked with Codex (codex review --uncommitted).

**Architecture shipped**:
- New jyagent/memory/event_log.py — append-only per-session JSONL at data/sessions/events/<sid>.jsonl. EventLog class with emit/emit_many (batched fsync), get_events (positional slice), monotonic seq.
- ConversationMemory now has _event_log + _recorded_seq cursor; attach/detach via lifecycle methods (NOT auto-attached in __init__/clear()).
- checkpoint_session flushes log FIRST (source of truth), then writes snapshot — invariant: snapshot can lag log, never lead it.
- Snapshot payload now includes `last_event_seq` for sync-detection.
- compaction emits a `{"kind":"compaction", "drop_count", "replacement_messages", ...}` event before the swap, preserving pre-compaction context AND the synthetic summary+file-reinjection messages.
- replay_from_events(sid) reconstructs the live view from the log alone (walks message events, applies compaction transformations).

**Codex's pre-review (planning phase) caught**:
1. My crash-analysis was wrong — extend()→crash before checkpoint loses both views, not just one. Per-turn boundary IS the durability unit.
2. Compaction event MUST embed replacement_messages (file-reinjection content not reconstructible from filesystem at replay time). Initial plan only had summary.
3. Use drop_count not range — unambiguous.
4. Batch fsync at checkpoint boundary, not per-event. emit_many() does one fsync per batch.
5. Event schema: `{"kind":"message","message":<full dict>}` not flattened — preserves tool_call_id, tool_name, is_error, etc.
6. Lazy attach — ConversationMemory()/clear() must NOT touch logs.

**Codex's post-review (codex review --uncommitted) caught 2 P2 correctness bugs**:
1. /new could be undone by /continue: archive_session+clear left latest.json pointing at the just-cleared session. Fixed: _cmd_new now also calls delete_session(). Archive still recoverable by ID via /sessions.
2. Log-ahead recovery silently masked orphan events: if process crashed between log flush and snapshot write, load_session attached the log but kept stale messages, and next checkpoint marked "synced" → orphan events lost forever. Fixed: load_session now detects log_ahead via len(log) > snap_seq+1, calls replay_from_events to recover the true view, surfaces "log_ahead_recovered" status to user.

**Tests**: 12 new in tests/test_event_log.py (EventLog unit tests, snapshot/log integration, compaction preserves history, replay roundtrip, log-ahead recovery, sync-baseline). 6 in tests/test_session_checkpoint.py (updated to handle events/ subdir). Full suite: 826 passed (1 skipped, 0 failures).

**Files diff**: 6 modified + 3 new (event_log.py, test_event_log.py, test_session_checkpoint.py). +361/-9 LOC excluding new files.

**Co-work observation**: Codex (background, codex-cli 0.128.0, ~5 min review) found two real correctness regressions that my own testing did NOT catch. Both were "user-visible behavior changes" the test suite couldn't detect because they require multi-process state transitions. Worth the round-trip cost for non-trivial refactors.

## 2026-05-06 17:25 [refactor]
2026-05-06: jy-agent session.py simplification, branch `session-simplify` (2 commits ahead of main, NOT merged).

Goal: drop snapshot dual-writer (latest.json + <ts>.json archives) so the per-session events/*.jsonl event log is the only persistent file. User asked.

Final design:
- events/<sid>.jsonl is the only persistent file. Plus latest.txt (atomic plain-text pointer holding the current session_id).
- New event kinds: session_start (seq 0, metadata), session_meta (on metadata change, deduped via per-log cache), session_end (reason="new", emitted by /new).
- API: save_session + archive_session removed. checkpoint_session does it all. end_session(conv, reason) replaces archive_session for /new. load_session(conv, query=...) accepts session_id / prefix / "latest" / None.
- Migration: legacy *.json → synthesized event log + rename to *.json.legacy. ONE-SHOT, idempotent, fails-safe (does NOT rename on synthesis failure — was a silent-data-loss bug I caught in self-audit).
- Crash safety: compaction now ABORTS the swap if log emit fails (was: silently proceeded). With log-only persistence, swap-without-event-emit would orphan pre-compaction context forever.

Codex review: timed out twice at 10min reading full diff (too large). Used codex for plan review (caught 6 real issues — incorporated all). Did final self-audit catching 3 more (silent migration loss, O(N) metadata dedup scan, drop_count clamp).

Tests: 863 passed, 4 skipped. New tests added for the perf invariant (metadata dedup is O(1) per checkpoint), legacy migration data-preservation under failure, end-to-end session lifecycle.

LOC: session.py grew 479 → 730. Reduction is in *invariants* (no more "log first, then snapshot" + "log_ahead_recovered" + last_event_seq bookkeeping), not lines. The new code carries explicit migration + recovery + summarization paths.

Branch left for user review/merge. Did NOT auto-merge because: (a) significant change deserves review, (b) the currently-running jy-agent CLI is on main and the migration would run on its next launch.

## 2026-05-07 13:52 [refactor]
Co-review with Codex of jyagent/ source structure → shipped 2 of 6 recommendations.

**Codex structure review** (saved at data/reviews/2026-05-06-codex-structure-review.md):
- 6 oversized files identified: tools/core.py (1535), tools/subagent.py (1321),
  runtime/loop/step.py (1107), memory/operations.py (1109), mcp/manager.py (858),
  agent.py (725), skills.py (626).
- Top boundary issues: dead engine.py back-compat aliases; ui/terminal.py reaches into
  private tools._TOOL_METADATA; atomic_write in tools/core.py used by memory/.
- Re-ranked Codex's #1 recommendation higher because the "engine alias" pattern had
  already caused a real bug (the C4 Phase 5 _execute_tools gotcha in MEMORY.md).

**PR1: refactor/drop-engine-aliases (commit 2f8405a)** — 24 LOC removed from
runtime/loop/engine.py. Killed 5 dead re-exports (execute_tool_with_timeout,
CostTracker, StuckLoopDetector, is_truncated, strip_dangling_verification). Updated 1
test that was reaching strip_dangling_verification via the `engine as le` alias.
867 passed, Codex review clean.

**PR2: refactor/split-tools-background (commit 75638b4)** — extracted 481-LOC
tools/background.py. tools/core.py: 1535→1087 (-29%). 2-line re-export shim in core.py
preserves the legacy import path for tools/__init__.py registration. Updated
test_background.py to import from the new module + repointed `bg_core` fixture alias.
867 passed, Codex review clean.

**Workflow notes that worked well**:
- git worktree per refactor (per durable rule) + `uv sync` in worktree → isolated venv
  so the running parent agent's editable install isn't disrupted.
- Pre-edit static audit: `grep -rE "(loop_engine|_engine|\\.engine)\\.${name}\\b"` to
  find all callers of each candidate alias before deletion.
- Codex review pattern: `codex review --uncommitted` after `git add -A` — note it
  buffers ALL output until completion (took ~15 min for PR2). Acceptable since it
  catches mistakes before commit.
- Re-export shims are the right tool for splits where many call sites would otherwise
  need touching (e.g. tools/__init__.py registration). Keep the shim, document why.

Both branches are sitting on local refactor branches, ready to merge when user
greenlights. Worktrees: ../jy-agent-refactor-engine, ../jy-agent-refactor-bg.

## 2026-05-07 15:20 [refactor]
PR3: refactor/utils-files (commit f5828f1) — recommendation #3 from Codex structure review.

Two boundary fixes in one PR:
1. Created jyagent/utils/ subpackage; moved atomic_write from tools/core.py to
   utils/files.py.  Kept top-level `from ..utils.files import atomic_write` in
   tools/core.py as a re-export shim for legacy callers.  memory/operations.py
   now imports from utils.files directly.
2. ui/terminal.py approval gate switched from private tools._TOOL_METADATA dict
   lookup to public get_registry().freeze().is_mutating(name) — same classification
   surface as the dispatch loop, so UI and loop can never disagree.

Diff: +91/-45 across 5 files (utils/__init__.py, utils/files.py, tools/core.py,
memory/operations.py, ui/terminal.py).  867 passed, 4 skipped — same baseline.

CODEX REVIEW QUIRK: codex review --uncommitted on this diff timed out at the
25-min deadline with EMPTY output buffered (same buffering behavior as my
durable rule about `claude -p --bare`). Fell back to `codex exec --sandbox
read-only` piping the staged diff on stdin — it actively investigated all
four review questions (verified via grep/sed traces in the output file) but
ALSO got killed at the deadline before printing the final verdict turn.

Lesson: for codex review on a refactor PR, prefer codex exec with a SCOPED
prompt (specific questions + "be terse" constraint) over codex review
--uncommitted (broad scope, slow, no streamed output). Both buffer until
completion — set a generous deadline AND structure the prompt for fast yes/no.

Status of the 6-item structure-review queue:
- #1 ✅ shipped (drop engine.py aliases — commit 2f8405a)
- #2 ✅ shipped (split tools/core.py → tools/background.py — commit 75638b4)
- #3 ✅ shipped (utils/files + UI metadata leak — commit f5828f1)
- #4 split memory/operations.py — pending, M/Med
- #5 factor Chrome out of mcp/manager.py — pending, M/Med
- #6 step.py phase extraction — deferred, L/High

## 2026-05-07 17:57 [refactor]
PR4: refactor/split-memory-ops (commit f87ec7b) — recommendation #4 from Codex
structure review.  Split memory/operations.py (1109 LOC) along the 3-tier
memory design into 4 leaf modules + facade:

  _index.py         (302 LOC) — Tier 1 MEMORY.md
  _topics.py        (455 LOC) — Tier 2 topics
  _journal.py       (154 LOC) — Tier 3 journal
  _consolidation.py (172 LOC) — dedup analyzer
  operations.py     (207 LOC) — facade with re-exports + high-level commands

Code now mirrors the documented memory architecture. Layering DAG is acyclic:
_consolidation/_topics/_journal → _index → config; operations.py → all four.

WORKFLOW NOTES — what worked vs what almost broke:

Bad pattern I caught early: when first building _index.py, I rewrote the
_validate_memory_entry function from memory instead of copying it verbatim
from the original file. Result: 28 tests immediately failed because the new
version had less strict validation (no multi-line check, no markdown-heading
check). Lesson: for refactor splits where behavior must be PRESERVED, ALWAYS
extract by `sed -n '<start>,<end>p' original > section_chunk` and assemble
files from those exact byte ranges. Never paraphrase.

Recovery: I rebuilt _index.py from sed-extracted ranges, ran a Python AST
diff (ast.get_source_segment per function), and verified ZERO body drift
across all 36 functions. Should have done this BEFORE the first test run.

Codex review (scoped `codex exec` per durable tip from PR3): completed in
~7 minutes (vs PR2/PR3 timing out at 25 min). It actively ran AST grep
scripts to verify Check #1 (every imported name re-exported) and found
two Low findings:
  1. 10 private constants (regexes, sentinels) not re-exported.
  2. _index.py had unused datetime/ZoneInfo imports.
Both fixed before commit.

Confirms the durable tip: a SCOPED prompt with specific yes/no questions
(checks 1-5) + "be terse" + piped diff on stdin produces a clean review in
~7 min vs ~25 min timeouts on broad `codex review --uncommitted`.

867 passed, 4 skipped — same baseline as main.

Status of the structure-review queue:
- #1 ✅ shipped (drop engine.py aliases — 2f8405a)
- #2 ✅ shipped (split tools/core.py → background.py — 75638b4)
- #3 ✅ shipped (utils/files + UI metadata leak — f5828f1)
- #4 ✅ shipped (split memory/operations.py — f87ec7b)
- #5 factor Chrome out of mcp/manager.py — pending, M/Med
- #6 step.py phase extraction — deferred, L/High

## 2026-05-07 18:39 [refactor]
PR5: refactor/mcp-chrome (commit 8c50f0c) — recommendation #5 from Codex
structure review.

Extracted all Chrome-specific code from mcp/manager.py (858 LOC) into a
new mcp/chrome.py module with a ChromeBrowser(manager) class.

Result:
  manager.py: 858 → 665 LOC (-193, -22%)
  chrome.py:  335 LOC (new)

manager.py is now genuinely browser-agnostic: it knows the registry of
pre-connect hooks and instantiates self._chrome = ChromeBrowser(self) but
all Chrome implementation lives in chrome.py.

KEY DESIGN DECISION — back-compat via test-mock routing:
tests/test_chrome_concurrency.py reaches deeply into the manager's Chrome
internals (69 references: mgr._chrome_acquire, mgr._chrome_refcount with
both read AND write, mgr._chrome_page_lock, mgr._chrome_fetch_page_inner,
mgr.chrome_ensure_connected). To preserve all of them with ZERO test
churn, the new code routes back through the manager's public/shim methods
at three points:
  - ChromeBrowser.acquire() → self._mgr.chrome_ensure_connected()
  - ChromeBrowser.fetch_page() → self._mgr._chrome_acquire/_release/_chrome_fetch_page_inner
This means mocking these methods on the manager intercepts the actual
call paths (tests previously relied on this).

Manager exposes:
  - Public delegators for tools/web_fetch.py: chrome_fetch_page,
    chrome_ensure_connected
  - Private shims for tests: _chrome_acquire/_release/_fetch_page_inner/
    _extract_js_result/_parse_chrome_page_id
  - @property proxies onto self._chrome for state: _chrome_page_lock,
    _chrome_refcount (WITH setter), _chrome_refcount_lock

WORKFLOW NOTE — caught a bug from paraphrasing fetch_page signature:
First shim attempt used (url, *, timeout, max_chars, extract_js, label)
but real signature is (url, *, timeout, js_function). Caught by tests
within seconds. Lesson reinforces the durable gotcha from PR4: when
preserving signatures, COPY them from the source — never paraphrase.

Codex review (~5 min, scoped codex exec): "No correctness findings."
Specifically verified: routed call chain has no recursion / double-locks;
no stale references in manager.py; import order safe (TYPE_CHECKING for
back-reference); chrome_fetch_page signature exactly preserved.

Also caught a trivial style nit (trailing blank line at EOF) that
git diff --check flagged — fixed before commit.

867 passed, 4 skipped — same baseline.

Status of structure-review queue:
- #1 ✅ shipped (drop engine.py aliases — 2f8405a)
- #2 ✅ shipped (split tools/core.py → background.py — 75638b4)
- #3 ✅ shipped (utils/files + UI metadata leak — f5828f1)
- #4 ✅ shipped (split memory/operations.py — f87ec7b)
- #5 ✅ shipped (extract mcp/chrome.py — 8c50f0c)
- #6 step.py phase extraction — DEFERRED, L/High effort, high risk

5 of 6 done. The remaining item (#6) is the L/High-risk one we deliberately
deferred — splitting runtime/loop/step.py (1107 LOC) is the heart of the
agent loop and warrants a separate, careful planning round.

## 2026-05-07 19:02 [ship]
Merged 5 refactor branches into main. Final commit: 2fe2dad.

Sequence: drop-engine-aliases → split-tools-background → utils-files →
split-memory-ops → mcp-chrome. Each merged with --no-ff to preserve a
distinct merge commit per PR for audit history.

Conflicts encountered:
  - PR3 vs PR4 on memory/operations.py: PR3 changed line 660 (atomic_write
    import path), PR4 deleted that line as part of the facade rewrite.
    Resolution: took 'theirs' (PR4 facade) since PR3's edit is now in
    _topics.py post-split. Also updated _topics.py's atomic_write import
    to use PR3's preferred jyagent.utils.files path (old path still works
    via re-export shim, but it's the deprecated one).
  - PR2 vs PR3 on tools/core.py: auto-resolved by ort strategy; PR2 deleted
    the bottom half (background section), PR3 modified top imports —
    non-overlapping.

LOC results (4 oversized files targeted by structure review):
  runtime/loop/engine.py:  704 → 680   (-24)
  tools/core.py:          1535 → 1053  (-482, -31%)
  memory/operations.py:   1109 → 206   (-903, -81%)
  mcp/manager.py:          858 → 665   (-193, -22%)

7 new modules created (total +1963 LOC, but with much better separation):
  tools/background.py:     481 LOC
  utils/files.py:          64 LOC
  memory/_index.py:        302 LOC (Tier 1)
  memory/_topics.py:       455 LOC (Tier 2)
  memory/_journal.py:      154 LOC (Tier 3)
  memory/_consolidation.py: 172 LOC
  mcp/chrome.py:           335 LOC

Final test status: 870 passed, 1 skipped (only the SKIP_NETWORK_TESTS one).
Per-branch baseline was 867/4; the +3 / -3 shift reflects environment-
dependent skips that pass in main's worktree but skipped in the dedicated
refactor worktrees.

All 5 worktrees removed, all 5 branches deleted (now -d safe since merged).
Working tree contains only the in-progress data/memory/ session updates
(MEMORY.md + journal/2026-05.md) which are restored from stash and unstaged.

Recommendation #6 (split runtime/loop/step.py, 1107 LOC) remains
deliberately deferred — heart of the agent loop, L/High effort/risk,
warrants its own planning round.

## 2026-05-07 20:10 [ship]
PR6: refactor/split-step (commit 066cc1a, merged as ab63d96) —
recommendation #6 from Codex structure review.  The DEFERRED L/High-risk
one — splitting runtime/loop/step.py (1107 LOC, the heart of the agent
loop) into 5 life-cycle-grouped modules.

WORKFLOW DECISION: consulted Codex on the split strategy BEFORE making
any changes, since this was the highest-risk file in the queue. Codex's
input changed my plan in 4 important ways:
  1. Outcome types must live with RunState (step_state.py), not in their
     own module — prevents helpers from importing back through step.py.
  2. _prepare_step_batch belongs in step_setup, not step_tools (it
     produces the batch consumed by both context construction and tool
     execution — coupling to setup is stronger).
  3. _execute_tool_round + _check_stuck_loop MUST stay together (denial-
     result synthesis intentionally feeds the stuck-loop signal).
  4. Rename step_metrics.py → step_bookkeeping.py (does more than metrics:
     assistant message mutation, reflection injection, checkpoint cadence).

Final layout:
  step.py              306 LOC (was 1107, -72%)  coordinator (run_step)
  step_state.py        280 LOC (NEW)             RunState + outcome types
  step_setup.py        171 LOC (NEW)             pre-LLM setup helpers (3)
  step_tools.py        261 LOC (NEW)             tool-round helpers (2)
  step_bookkeeping.py  228 LOC (NEW)             post-LLM helpers (4)

Layering DAG (acyclic): leaf modules → step_state → llm_types/tools.registry.
step.py → all four leaves. NEVER the reverse.

KEY RISK MITIGATION: Codex's planning consult flagged "wrapper-based
re-exports breaking inspect.getsource" as the highest risk. Five test
files use inspect.getsource(step._helper) to assert source-text
invariants. Mitigated by using simple `from .leaf import _helper`
re-exports (NO functools.wraps, NO factory wrappers) — tested + verified
identity preservation: `step._helper is leaf._helper` for all 9 helpers.

VALIDATION:
- AST diff: all 14 original top-level functions/classes present, ZERO body drift.
- Codex's "fastest signal" tests (test_phases, test_reflection,
  test_loop_engine_critical, test_step_runner, test_tool_pre_execute_gate):
  110/110 pass.
- Full suite: 867/4 on branch, 870/1 after merge to main (env-skip shift).
- Codex review (~4 min, scoped codex exec on staged diff): 2 lint findings
  (Any unused in step_setup.py, stale top-level imports in step.py), both
  fixed. No correctness findings.

WORKFLOW NOTES — what worked:
- AST-based extraction (ast.get_source_segment per function) yielded 14 functions
  with zero body drift, on the first try. Honored the durable gotcha from PR4.
- Pre-flight Codex consult on split strategy was high-leverage: caught 4 issues
  I would have hit during execution. ~2.5 min for the consult vs. probably hours
  of rework if I'd executed my original plan and discovered the issues mid-PR.
- AST identity check (`step._helper is leaf._helper`) is the right smoke test
  for re-export refactors — catches both wrapper accidents and missing names
  in one assertion.

ALL 6 STRUCTURE-REVIEW RECOMMENDATIONS NOW SHIPPED:
- #1 ✅ drop engine.py aliases (2f8405a → df9ffc4)
- #2 ✅ split tools/core.py → background.py (75638b4 → 6f4ff8f)
- #3 ✅ utils/files + UI metadata leak (f5828f1 → ec5fb14)
- #4 ✅ split memory/operations.py (f87ec7b → df27858)
- #5 ✅ extract mcp/chrome.py (8c50f0c → 2fe2dad)
- #6 ✅ split runtime/loop/step.py (066cc1a → ab63d96)

Final main test status: 870 passed, 1 skipped (only SKIP_NETWORK_TESTS).
LOC reductions on the 5 originally-oversized files:
  runtime/loop/engine.py:    704 → 680   (-3%)
  runtime/loop/step.py:     1107 → 306   (-72%)
  tools/core.py:            1535 → 1053  (-31%)
  memory/operations.py:     1109 → 206   (-81%)
  mcp/manager.py:            858 → 665   (-22%)

Total source lines added: ~3000 in 11 new leaf modules; total source lines
removed from oversized files: ~3300. Net negative (genuine refactor, not
just shuffling), with much better separation of concerns + every original
behavior preserved (zero AST body drift across all 6 PRs).

## 2026-05-08 12:53 [memory_revision]
Tightened the worktree-discipline rule. Old rule said "use git worktree for self-upgrades" but was size-gated by my own judgment ("this feels small, skip the ceremony") — failed today: edited engine.py + 5 test files directly on main while running jy-agent loaded from main. Risk was nil this time (comment+test-only diff, in-memory bytecode untouched), but process violated the rule. New rule: unconditional `git branch --show-current` check before any edit under `jyagent/**.py`. Recovery: `git checkout -b refactor/retire-toolcallrequest-shim`, staged the 6 intended files explicitly (kept pre-existing MEMORY.md + journal/2026-05.md changes out of the commit per the "silent drop" gotcha), commit 876ce7a, back to main.

## 2026-05-08 16:43 [refactor]
Reviewed all 68 "back-compat" / "legacy" / "deprecated" mentions in jyagent/ (24 files). Categorized into A) truly dead with zero callers, B) load-bearing imports with misleading "for back-compat" comments, C) legitimate behavioral compat / historical notes. Two cleanup commits on main:

* 3b34a17: deleted llm/history.py + llm/reasoning.py whole-file shims; removed _anthropic_helpers.py back-compat re-export block (kept only transform_messages_for_target which is used in same file); removed _OPENAI_LEGACY_REASONING_MODEL_PREFIXES + uses_openai_legacy_reasoning_transport (function's own docstring conceded "natively supported" — obsolete by self-admission). -62 LOC.

* 4fbdbc8: removed dead 'executor' param from execute_tool_with_timeout (its own docstring said "now ignored"); removed SKIP_DIRS/BINARY_EXTS re-export from tools/__init__.py; reworded misleading "for back-compat" comments on imports that turned out to be load-bearing (LoopCallbacks/Config/Result re-exports in engine.py, AnthropicReasoningConfig in llm/types.py, atomic_write in tools/core.py, TerminalRenderer/console in ui/cli.py, _PROTECTED_SECTION_HEADERS comment in memory/extraction.py).

All 870 tests passed after both commits. Method that worked well: for each match, grep the symbol across jyagent+tests, then classify: 0 callers = delete; internal-only callers = comment is wrong; documented user-facing escape hatch (e.g. JY_SUBAGENT_FLAT_RESULT, memory_mode='full') = keep.

## 2026-05-08 17:26 [refactor]
Continued back-compat audit after user pushback on 'legitimate behavioral compat' bucket. 4 more commits on main:

* 767280a: dropped JY_SUBAGENT_FLAT_RESULT env-var escape hatch (added 3 weeks ago, never used outside its own tests). −44 LOC.

* 0f431bb: dropped memory_mode="full" escape hatch (added 10 days ago when default flipped, never opted into). Schema enum now {none, matched}. The 'matched' mode is strictly better than the verbatim 4KB MEMORY.md dump it would have replaced. −36 LOC.

* c89ff14: rewrote misleading 'legacy fallback' / 're-export for back-compat' comments on code that turned out to be load-bearing. The +1-per-dispatch floor in stats.add_subagent_usage is a defensive guard against malformed bg outcome dicts, not a compat shim. The step.py imports of RunState/Step* and 9 _<helper>s are actually used inside run_step's body — not re-exports. Dropped now-incorrect '# noqa: F401' tags.

* cc88fa4: collapsed the tools/core.py → tools/background.py re-export indirection (only caller was tools/__init__.py — which now imports directly from .background). Reworded last 2 'legacy' comments in subagent._make_subagent_outcome and step_state.py module docstring.

Remaining 'back-compat'/'legacy' hits in jyagent/: 16 (down from 68). All accurate descriptions of current behavior, historical breadcrumbs, or rationale comments — none are dead code.

Big lesson revising my earlier 'documented & tested = legitimate' rule: in a single-user agent there is NO external API contract. A test that exercises an escape hatch is evidence somebody once thought it might be needed, NOT evidence anyone uses it. The actual question to ask is: 'who calls this, and what would break if it disappeared?' I had to be challenged twice (by the user) before I applied this rigorously to JY_SUBAGENT_FLAT_RESULT and memory_mode='full' — both classic 'recently added with good intentions, never adopted' escape hatches.

Pattern noticed in this codebase: comments that say 'kept for back-compat' or 'legacy callers' are very often inaccurate self-narration of a refactor — the imports/methods are actually load-bearing for the same module's own body. Verifying with a grep for actual external callers takes 2 minutes and routinely changes the verdict from 'shim to remove' to 'comment to fix'.

Cumulative session impact (this conversation): 7 commits, ~−170 LOC of dead/misleading back-compat code, 867 tests still pass.

## 2026-05-09 11:51 [ship]
2026-05-06 — Shipped `fix/loop-latent-bugs` (f5aad86) off main. Three independent latent bugs:

1. **Verification re-arming**: one-shot `verification_injected: bool` → index-based `last_verification_idx: int | None`; gate now re-fires on NEW mutations after a verification. Dropped redundant `_already_injected` call from `should_verify` (was actively wrong — Anthropic tool_result messages have role=user so the "most recent user" probe shadowed real mutation rounds).
2. **Tool-call ID collision**: `step_tools._execute_tool_round` keyed by `block.id` (provider string) → `id(block)` (Python object identity). Fixes silent collapse when two distinct ToolCallRequest objects carry the same provider-assigned id.
3. **Shallow input copy**: `dict(tool_input)` (shallow, cancel-event branch only) → `copy.deepcopy(tool_input)` unconditionally + explicit `_cancel_event` kwarg.

All 876 tests pass + 9 new pinning tests in `tests/test_loop_latent_bugs.py`. Codex review hit 600s deadline mid-exploration; its one concrete concern (compaction shifting indices) verified moot by inspecting `compact_messages` — it's content-only, preserves `len(messages)`.

Branch board across session:
- `fix/loop-runtime-safety` (3c60081) — 4 defects, off main
- `refactor/loop-retry-consolidation` (1c20f9d) — off main
- `refactor/split-tool-executor` (55be4b6) — off main
- `refactor/contextvar-propagation` (8fa339e) — stacked on #3
- `fix/loop-latent-bugs` (f5aad86) — off main, independent

All 5 branches independent of each other except #4 stacks on #3.

## 2026-05-09 12:28 [ship]
2026-05-06 — Merged all 5 branches to main. Merge order:
1. fix/loop-runtime-safety (afeba98) — clean
2. refactor/loop-retry-consolidation (9d2b153) — auto-merged on engine.py, llm_runner.py
3. fix/loop-latent-bugs (340aa6f) — auto-merged on tool_executor.py
4. refactor/split-tool-executor (0d3791b) — 1 conflict on tool_executor.py (facade vs monolith); resolved by taking split's facade + porting #5's deepcopy fix into tool_invocation.py
5. refactor/contextvar-propagation (439a657) — 1 trivial conflict on tool_invocation.py imports (copy + contextvars both added); resolved by keeping both

Final: 911 passed + 1 skipped. main is 35 commits ahead of origin/main. 5 merged branches deleted.

Total session output: 5 logical PRs covering the entire Codex C4 review board except #8 (budget-driven compaction, still open).

## 2026-05-09 12:45 [decision]
2026-05-06 — User declined #8 (Budget-driven multi-pass compaction) from the C4 review board. Reason not specified; likely overkill for current workloads. Do NOT re-propose this as work. The existing single-pass compact_messages (content masking tier 0/1/2 in runtime/loop/compaction.py) is sufficient.

C4 review board officially closed. All 4 refactor items + 4 critical/high fixes + 3 latent bugs merged to main (35 commits ahead of origin/main). 911 tests pass.

