# Journal — 2026-04

Tier 3 / append-only / NEVER auto-loaded. Chronological session notes live here so they don't pollute MEMORY.md (the always-loaded index) or topic files (curated knowledge).

## 2026-04-24 18:21 [ship]
Shipped memory-system 3-tier redesign (branch: memory-redesign).

CHANGES:
- config.py: added JOURNAL_DIR + MEMORY_INDEX_WARN_LINES / _BYTES soft caps
- memory/operations.py: new append_journal/list_journals/read_journal, memory_index_size_warning, consolidate_memory; remember() now surfaces warning when approaching cap; show_memory() lists journals and appends warning
- memory/__init__.py: re-export the new API
- tools/facades.py: new manage_memory actions (journal, consolidate); action=note now redirects to journal (was silently appending to MEMORY.md)
- tools/schemas.py: rewrote manage_memory description to teach the three-tier model
- agent.py + memory/context.py: system prompt explains the tier model and routing rules

DATA MIGRATION:
- MEMORY.md: 67L/9KB -> 62L/5KB; extracted the 1500-char [note] 2026-04-18 Agent-loop dump + long run_background/skill-router/GFW tips into:
  - topics/memory-design.md (new - records this design + rationale)
  - topics/agent-loop-changelog.md (new)
  - topics/skill-router-fix.md (new)
  - topics/gfw-proxy-fallback.md (new)

TESTS:
- tests/test_memory_tiers.py (new, 21 tests): journal tier isolation from MEMORY.md, size-warning thresholds, consolidate analyzer, facade action routing incl. note->journal deprecation, show_memory journal surfacing
- Full suite: 492 passed, 1 skipped (was 404).

LESSONS:
- Anthropic/Letta/Mem0/LangMem/Zep/A-MEM unanimous: the always-loaded tier is for durable rules ONLY; chronological notes go to a never-auto-loaded tier. NoLiMa shows Claude 3.5 effective context is ~4K tokens; Chroma context rot confirms distractors compound.
- Self-caught irony: my old MEMORY.md already had the rule 'Mutating Anthropic system_prompt breaks prompt caching; inject dynamic context as a non-persisted tail message block instead.' and I was violating it by appending [note] entries. Kept that rule in the lean MEMORY.md + cross-referenced in topics/memory-design.md.

## 2026-04-25 14:45 [ship]
Shipped web_search cascade upgrade (commits b1a614e, b4617bb on main, +11 ahead of origin)

Background: user asked me to evaluate web_search (DDG-only HTML scrape) and research how popular AI agents do search. Key external findings:
- Claude Code WebSearch returns title+URL only (drops encrypted_content), Anthropic's server-side tool ~Brave-powered, $10/1k searches (mikhail.io reverse-eng).
- ChatGPT/o3-deep-research grounds on Bing Search.
- Cursor/Cline use Tavily or SerpAPI.
- OSS agents (OpenClaw, Open WebUI) standardize on SearxNG self-hosted.

Decisions:
1. Surgical Chrome-tier removal: deleted _CHROME_EXTRACT_SEARCH_JS + _STRATEGY_MAP_SEARCH from web_fetch. Kept _fetch_chrome as last-resort tier-4 for JS-heavy non-search pages (Twitter/Reddit/Zhihu/Weibo/Bilibili). Chrome SERP scraping was slow (3-10s vs 300ms), serialized on single browser, defeated by Google bot detection.
2. New web_search cascade: SearxNG (env-gated) → DDG → Brave → Mojeek. First engine returning ≥3 results wins. WEB_SEARCH_ENGINE env forces single engine.
3. Verified all selectors against live HTML 2026-04-25:
   - Brave: a.l1 is the stable anchor (svelte-<hash> classes rotate, ignore them)
   - Mojeek: ul.results-standard > li > h2 a.title + p.s for snippet
4. Brave HTML rate-limits aggressively (HTTP 429) from cloud IPs and intermittently from residential — cascade absorbs this cleanly.

Tests: 13 new unit tests + 1 live integration. All 468 tests in suite pass.

Operational note for user: to enable SearxNG, `docker run -d -p 8888:8080 searxng/searxng` then `export SEARXNG_URL=http://localhost:8888`. DDG remains zero-config default.

Not pushed — user only asked to commit.



## 2026-04-26 — Renamed jyagent/runtime → jyagent/llm (+ Runtime* identifiers)

User asked: "is 'runtime' a proper name for your runtime package?" — pushed back honestly: no, the package only holds LLM provider adapters (Anthropic Messages, OpenAI Responses) + shared message/streaming types + a thin dispatch class. The actual agent runtime (loop, phases, reflection, todos, checkpoint, MCP, skills, tools) lives at the top level of `jyagent/`. The name "runtime" collided with the conventional "agent runtime" meaning and misled readers.

Executed on worktree `/tmp/jy-agent-llm` (branch `refactor/runtime-to-llm`):

**Commit 1 — package rename** (`73b3602`):
- `git mv jyagent/runtime → jyagent/llm` (13 files, history preserved)
- `git mv tests/test_runtime.py → tests/test_llm.py`
- Import rewrites: `jyagent.runtime` → `jyagent.llm`, `from .runtime` → `from .llm`, `from ..runtime` → `from ..llm`
- Logger channel `"jyagent.runtime"` → `"jyagent.llm"` (must match package)
- 1 stale path docstring in `_anthropic_reasoning.py`
- pyproject.toml unchanged (no entry-point)

**Commit 2 — identifier rename** (`cf86451`):
- `RuntimeOwner` → `LLMOwner` (kept "Owner" suffix to preserve the "owns the active model_spec" semantic — `switch_model()` makes this meaningful; declined `LLMClient` despite it being industry convention)
- `RuntimeOptions` → `LLMOptions`
- `RuntimeAdapter` (Protocol) → `ProviderAdapter` (more accurate — adapts a *provider*)
- `RuntimeStream` → `LLMStream`
- field `runtime_warnings` → `llm_warnings`
- tracing key `"runtime_owner"` → `"llm_owner"`
- error string `"Unknown runtime provider"` → `"Unknown LLM provider"`

Tests: 486 passed, 1 skipped (baseline preserved at every step). Two-commit split done by reverting identifier subs in working tree, committing the package rename, then re-applying — clean diffs of 24 files / +144/−144 total.

Naming candidates considered for the package: `llm/` (chosen — matches LangChain `langchain_core.language_models`, LlamaIndex `llms/`, Vercel `ai/`), `providers/` (under-sells shared types), `models/` (overloaded with data models), `inference/` (uncommon for SaaS-API wrappers), `chat/` (understates streaming/reasoning/usage layer).

Open question for user: whether to also rename `LLMOwner` → `LLMClient` for full industry-convention alignment. Trade-off is losing the explicit "owner of mutable model state" signal. Not done — flagged in the chat report.

MEMORY.md line 46-47 updated on the same branch (so it merges atomically). NOT yet merged to main — user makes that call.
## 2026-04-25 16:49 [ship]
Built `jyagent/runtime/` package — extracted the in-tree agent runtime into a cohesive library surface.

**Branch**: `refactor/runtime-package` in worktree `../jy-agent-runtime-refactor`. 4 commits, ready to merge.

**Phases**: 1 (move 12 leaf helpers) → 2 (move loop_engine + extract LoopCallbacks) → 3 (carve LoopConfig/LoopResult) → 4 (audit shims, folded into 1+2) → 6 (README docs). Phase 5 (Codex review) only got partial output before Codex died on network errors twice — partial findings extracted and documented.

**Process notes**:
- Delegated phases 1-4 to Claude Code in background (`-p --bare`, 12 min, succeeded). Output buffered until done — used `git status` polling to verify real progress.
- Codex CLI died mid-summary on `network error: error decoding response body` both times (after 55k and 85k tokens of useful probing). Pattern matches the GFW issue — likely needs the SSH SOCKS5 fallback. Useful findings still extracted.
- Wrote topic doc `data/memory/topics/runtime-refactor.md` BEFORE delegating, but didn't realize the worktree wouldn't have it (separate filesystem state) — Claude Code worked from the inline brief instead. Future: write planning docs into the worktree, not main.
- Caught my own README example fabricating a non-existent `LLMOwner.from_env()` API (and wrong `LoopCallbacks` shape — it's a dataclass with optional callable fields, not a subclassable class). Honesty principle: ran the example through `uv run python` BEFORE committing; fixed three signature mistakes; verified 8/8 public API exports + 16 tools register.

**Key Codex finding worth remembering** (already in topic doc): shims do `globals().update(__dict__)` once at import — classes/singletons stay shared (`old.X is new.X`) but module-level **mutable constants** diverge. So `monkeypatch.setattr('jyagent.verification.VERIFICATION_ENABLED', True)` is a silent no-op because `should_verify()` reads the NEW module's globals. Documented in README. No current tests bitten.

**Result**: 13 files moved (`git mv`, history preserved), 5 new files, 13 backward-compat shims, 486 tests passing throughout, no test files modified.

## 2026-04-25 17:03 [debug]
Post-merge fallout from runtime-package refactor — and the lesson it taught.

**What broke after merge** (despite "486 passed" in the worktree):
- 10 tests failed: monkey-patches against shim paths (`mock.patch('jyagent.verification.VERIFICATION_ENABLED', ...)`) — exactly the shim-asymmetry pitfall Codex flagged
- 1 silent runtime bug: `TRACES_DIR = Path(__file__).resolve().parent.parent / "data" / "traces"` — broke when tracing.py moved from `jyagent/` (depth 1) to `jyagent/runtime/loop/` (depth 3). Trace files leaked into `jyagent/runtime/data/traces/` for the duration. 17 files moved back.

**Why my "486 passed, independently verified" was wrong**:
The editable install in `.venv` (the only venv) hardcodes:
```python
MAPPING = {'jyagent': '/Users/jyxc-dz-0100398/jy-agent/jyagent'}  # MAIN worktree path
```
So `uv run pytest` from the refactor worktree imported `jyagent` from the main worktree's pre-refactor source. The tests "passed" because they were exercising the OLD code, not the refactored code. Both Claude Code's verification and my "independent" verification were testing the same wrong thing.

This is now a durable rule in MEMORY.md (gotcha): "git worktree shares the parent repo's editable-install `.venv`…"

**Fixes** (3 commits after merge):
- `bf0640d` fix(tests): mechanical patch-string substitution + one alias change in 4 test files (20 lines total)
- `91be7e0` fix(runtime): TRACES_DIR uses `parents[3]` instead of `parent.parent`

**Final state**: 486 passed, 1 skipped (verified in true post-merge tree). 7 commits added to main since the refactor branched. README's "shim asymmetry" gotcha note is now battle-validated.

**Honesty correction**: my prior message said the refactor was "merged cleanly" — it wasn't. It merged via fast-forward, but immediately broke 10 tests + leaked trace files to the wrong directory. Reported the failures within seconds of detection rather than papering over.

## 2026-04-25 17:11 [debug]
Fixed skills discovery bug: `DEFAULT_SKILLS_DIR` in `jyagent/runtime/skills.py:38` used 2 `dirname()` calls (correct for old `jyagent/skills.py` location) but after the runtime refactor moved the file one level deeper, it resolved to `jyagent/skills/` (nonexistent) instead of `<repo>/skills/`. Symptom: `manage_skills list` returned "No skills found" despite 7 skills on disk. One-line fix: add third `dirname()`. Verified fresh import discovers all 7. Documented in topics/runtime-refactor.md with audit checklist for future package-deepening refactors. User caught this — it had been latent since the refactor merged.

## 2026-04-25 17:29 [ship]
Runtime-refactor finalized: removed all 13 backward-compat shims (commit 10e7982).
- Deleted: jyagent/{loop_engine,checkpoint,phases,reflection,registry,remediation,session_stats,skills,todos,toolresult,tracing,validation,verification}.py
- Updated 11 test files + 2 skill scripts + README (mechanical import swap from jyagent.X to jyagent.runtime.[loop|tools|]/X)
- Net diff: -58/+34, 13 files deleted
- Verification: 489 passed, 1 skipped, 0 DeprecationWarnings (was 11 with shims still present)
- Public API confirmed: AgentLoop.__module__ == jyagent.runtime.loop.engine; get_registry().__class__.__module__ == jyagent.runtime.tools.registry
- Earlier in session: pushed runtime-refactor commits to origin (df62933) + added skill-default-path smoke test (a30e3d7).
- Gotcha learned: my first regex pass missed `from jyagent import X as alias` patterns (only caught `from jyagent.X import ...`). pytest collection failed on 4 files, fixed in a second pass. For future bulk-rename refactors, audit BOTH import forms.
Topic file `data/memory/topics/runtime-refactor.md` marked COMPLETE.

## 2026-04-25 17:53 [ops]
Patched local SearxNG (container `jolly_shaw`, image `searxng/searxng`, port 18888) to enable JSON format. Root cause: default `settings.yml` only enables `formats: [html]`, so `web_search`'s SearxNG tier silently 403'd and fell through to DDG. Added `- json` to the formats list (in /etc/searxng/settings.yml on anon volume 051f157c…), backup at settings.yml.bak.20260425-095313, restarted container. Verified: `web_search` now reports `Engine: searxng`. Note: /etc/searxng is on an UNNAMED docker volume — if container is recreated without remounting that volume, patch is lost.

## 2026-04-25 17:56 [ops]
Recreated SearxNG container with NAMED volumes. Old container `jolly_shaw` (anon volumes 051f157c… for /etc/searxng and 412da72e… for /var/cache/searxng) replaced with new container `searxng` mounting `searxng-config` + `searxng-cache`. Data copy used `searxng/searxng` itself as the helper image (alpine pull failed — Docker Hub auth.docker.io EOF, GFW). JSON-format patch in settings.yml carried over; backup file (settings.yml.bak.20260425-095313) also preserved. `web_search` end-to-end still routes via SearxNG. Old anon volumes left orphaned for user-confirmed cleanup. Recreate command: `docker run -d --name searxng -p 18888:8080 -v searxng-config:/etc/searxng -v searxng-cache:/var/cache/searxng searxng/searxng`.

## 2026-04-25 18:05 [ops]
Migrated SearxNG to docker compose. File: ~/searxng/docker-compose.yml. Uses external volumes (searxng-config, searxng-cache) so the existing data + JSON-format patch are adopted with zero migration. Added healthcheck on /healthz and switched restart policy from `no` to `unless-stopped`. `web_search` end-to-end still routes via SearxNG. Commands: `cd ~/searxng && docker compose up -d|down|logs -f`. Possible follow-ups (not done): move ~/searxng into git, switch settings.yml to a bind-mount for tracked reproducibility, add Valkey/Redis cache (matches official searxng-docker stack).

## 2026-04-25 18:08 [ops]
Moved SearxNG compose stack into jy-agent repo: ~/searxng/ → jy-agent/infra/searxng/. Files: docker-compose.yml + README.md. Stack recreated cleanly (volumes external — survived). Compose project name still "searxng" (matches container_name). New `infra/` directory is the first under jy-agent/ — convention established for future infra deps. Untracked in git, awaiting user commit.

## 2026-04-25 18:15 [ops]
Converted SearxNG settings.yml from in-volume to bind-mount. Workflow: edit jy-agent/infra/searxng/settings.yml on host → `docker compose restart` → done. Git tracks the file (visible diffs). Bind mount sits ON TOP of the searxng-config volume mount, so backup files + future state stay in the volume out of git. Verified: host md5 == container md5; instance_name change reflected after restart; web_search still routes via SearxNG. Repo dir now: docker-compose.yml + README.md + settings.yml.

## 2026-04-25 18:20 [review]
Ran codex-cli review of jyagent/runtime/ design + implementation.
- First attempt (broad single review) died at 103k tokens on network errors mid-summary (same pattern as runtime-refactor.md history). Recovered 3 useful signals from probing.
- Switched to 3 narrow scoped reviews with explicit "synthesize as you go" directive. 2/3 finished cleanly, 1 died after emitting findings but before TOP 3 RISKS (still recoverable).
- Total: 34 file:line-cited findings + 3 prioritized refactors. Saved to topics/runtime-review-2026-04-25.md.
- Top 3 systemic issues: (1) daemon-thread tool timeout leaks state via late side effects, (2) `[VERIFICATION]` cleanup missing on 3 exit paths (cost_limit, repeated_truncation, cooperative_cancel), (3) registry/stats reads bypass write locks.
- Next refactors ranked: verification-cleanup helper, atomic per-batch tool-metadata snapshot, lift SkillManager out of runtime/.

## 2026-04-25 18:23 [ops]
Slimmed jy-agent/infra/searxng/settings.yml from 67733 → 1162 bytes (98% reduction) using SearxNG's `use_default_settings: true`. Repo file now contains only 3 overrides: server.secret_key (mandatory — upstream default `ultrasecretkey` refused at startup), search.formats=[html,json] (mandatory for web_search JSON tier), general.instance_name (cosmetic). Inheritance verified: 281 engines, full UI/network defaults all picked up from image. Also dropped `:ro` from the bind mount because entrypoint chown warning was noisy. Container healthy, web_search routes via SearxNG.

## 2026-04-25 18:31 [ship]
Shipped refactor #1 from runtime-review-2026-04-25: `_finalize_run()` funnel.

- Branch: `fix/runtime-finalize-funnel` (commit a043259, +192/-72)
- Added module-level `_finalize_run()` helper next to `_strip_dangling_verification`
- Funneled all 9 `AgentLoop._run_impl` exit paths through it: cost_limit, completed (no-tools), truncation-error, dedup_break, cooperative-cancel, max_steps fallback success, max_steps fall-through, KeyboardInterrupt, Exception
- Fixes 5 bugs at once: 3 missing strip-dangling-verification (cost_limit, repeated_truncation, cooperative_cancel) + 2 missing trace.finish (truncation-error, fallback-success). Plus fired on_tool_end on cancellation-during-tool-batch (Part 3 #8).
- Updated 4 source-grep tests to assert funnel architecture instead of literal helper call.
- Added 2 new tests: `test_no_bare_LoopResult_returns_in_run_impl` (static funnel invariant — regex asserts no `return LoopResult(` survives in `_run_impl`) and `test_finalize_run_always_strips_dangling_verification` (behavioural across all 6 status values).
- Result: 491 passed, 1 skipped (unchanged). Updated topic file marking refactor #1 done.
- Branch not yet merged — left on `fix/runtime-finalize-funnel` for user review.


## 2026-04-25 18:59 [ship]
Shipped refactor #2 from runtime-review-2026-04-25: atomic per-step `ToolBatch` snapshot.

- Branch: `fix/runtime-tool-batch-snapshot` (commit 6c453fe, +588/-94 across 5 files)
- Branched off main (NOT off the funnel branch) so each PR is independent.

What changed:
- Added `ToolBatch` frozen dataclass in `jyagent/runtime/tools/registry.py` — bundles version, schemas (deep-copied), schema_map, functions, parallel_safe (frozenset), timeout_hints, large_input_keys (frozensets), compaction_priority. Has registry-shaped read methods so callers can swap registry → batch with no method changes. Plus `empty()` and `with_overlay()` builders.
- Added `ToolRegistry.freeze()` returning a `ToolBatch` (deep-copies schemas under `_lock`).
- Refactored 5 dispatch/compaction helpers in engine.py to consume `batch` instead of `(functions, registry)`: `_execute_tool`, `_execute_tools`, `_execute_tool_with_timeout`, `_compact_messages`, `_truncate_tool_call_blocks`.
- `_run_impl` now builds `tools_batch` once per step (via `freeze()` or from `_tool_source` enriched with `freeze()` metadata). write_todos overlay via `with_overlay()`. The `_tool_source` path also goes through ToolBatch so MCP tools get consistent metadata.
- Defense-in-depth: registry's individual metadata getters now also take `_lock` (Codex Part 1 #5 was about the same thing on stats, fixed analogously here).
- Kept `ToolRegistry.snapshot()` as a legacy shallow API for any external callers that haven't migrated.

Tests:
- Replaced `_FakeRegistry` duck-typed stub with `_make_batch()` helper that builds a real `ToolBatch` from a real `ToolRegistry.freeze()`. Tests now exercise the production code path, no mock surface drift.
- Updated 8 call sites for the new helper signatures.
- Added `TestToolBatchSnapshotIsolation` with 3 race tests covering Codex findings #4, #11, #12:
  * `test_freeze_is_atomic_under_concurrent_registration` — 200 freezes vs. churning register/unregister thread; every batch internally consistent.
  * `test_batch_isolates_schema_from_post_freeze_mutation` — mutating registered schema dict post-freeze must not leak.
  * `test_dispatch_partition_consistent_when_parallel_safe_flips_mid_step` — under high-rate parallel_safe flips, `_execute_tools` partition stays consistent.
- Result: 492 passed (was 489, +3 new), 1 skipped.

Topic-file update for refactor #2 is pending — file `runtime-review-2026-04-25.md` lives on `fix/runtime-finalize-funnel` (commit 931db72), not on main yet. Whichever PR merges last should add the "✅ DONE" marker for refactor #2 in the same PR or a follow-up doc commit.

Branch left at 6c453fe for user review. Both branches still pending review/merge:
  * fix/runtime-finalize-funnel (a043259) — refactor #1
  * fix/runtime-tool-batch-snapshot (6c453fe) — refactor #2

Next on the menu:
3. Lift `SkillManager` out of `runtime/` (Part 3 #3 #4) + extract `LLMClient` Protocol (Part 3 #5).

## 2026-04-25 19:14 [ship]
Shipped refactor #3 from runtime-review-2026-04-25 in two commits.

Branch: `fix/runtime-skill-extraction` (off main).

**Part A — Lift SkillManager out of runtime/** (commit c8a0cc0)
- `git mv jyagent/runtime/skills.py jyagent/skills.py` (history preserved per `git log --follow`)
- Internal-import depth fixes: `..config` → `.config`, `..llm` → `.llm`
- Critical: __file__-derived `DEFAULT_SKILLS_DIR` dirname count adjusted from 3 → 2 (file moved one level shallower). Same gotcha class as the 2026-04-25 bug. Smoke-tested: discovered all 7 skills.
- 6 caller files updated: agent.py, tools/facades.py, README, 2 skill scripts, 2 test files (1 import + 8 patch() targets via sed).
- Updated runtime/__init__.py docstring to remove "skills" from the runtime's stated scope.
- 489 passed, 1 skipped (unchanged).
- Out-of-scope follow-up flagged in commit message: SkillManager._route_llm still instantiates LLMOwner and prints colored stderr UX (Codex Part 3 #4) — separate "router interface + observer callback" PR.

**Part B — LLMClient Protocol** (commit 4046792)
- Engine actually used only 3 things from LLMOwner: `model_spec`, `complete()`, `stream()`. Defined `LLMClient` Protocol in `jyagent/runtime/loop/llm_client.py` documenting that surface.
- Engine now imports `LLMClient` for behaviour, keeps `LLMOptions`/`ModelSpec` as type-only imports from `jyagent.llm.types` (with explicit comment: bag-of-fields shared types, moving them is a separate larger refactor).
- 2 type annotations updated: `_build_runtime_options(runtime_owner: LLMOwner)` → `LLMClient`, and `AgentLoop.__init__(runtime_owner: LLMOwner)` → `LLMClient`.
- Verified `LLMOwner` satisfies `LLMClient` structurally (`isinstance(owner, LLMClient) is True`) — no caller change needed.
- Exposed `LLMClient` from `jyagent.runtime` public API.
- Added 3 contract tests in `tests/test_llm_client_protocol.py`:
  * `test_llmowner_satisfies_llmclient_protocol` — non-regression guard
  * `test_fake_llmclient_can_drive_agentloop` — liveness: a pure fake that doesn't import LLMOwner drives AgentLoop to completion
  * `test_llmclient_protocol_minimal_surface` — guards against Protocol feature-creep (asserts surface = {model_spec, complete, stream})
- 492 passed (was 489, +3 new), 1 skipped.

**Status of all three review-recommended refactors:**
| # | Branch | Commit(s) | Status |
|---|---|---|---|
| 1 | fix/runtime-finalize-funnel | a043259 | shipped, awaiting review/merge |
| 2 | fix/runtime-tool-batch-snapshot | 6c453fe | shipped, awaiting review/merge |
| 3 | fix/runtime-skill-extraction | c8a0cc0, 4046792 | shipped, awaiting review/merge |

All three branches are independent (each branched off main); they touch
different parts of the codebase and should merge cleanly in any order.

Topic file `runtime-review-2026-04-25.md` lives on the funnel branch
(commit 931db72) — whichever PR merges last should add the "✅ DONE"
markers for refactors #2 and #3 on that file in a follow-up doc commit.

## 2026-04-25 19:23 [ship]
Merged all 3 review-recommended refactors into main.

Sequence:
- `git merge --ff-only fix/runtime-finalize-funnel` — fast-forward (brought a043259 + 8abbe8f searxng + 931db72 docs along since they were stacked on the funnel branch). 491 passed.
- `git merge --no-ff fix/runtime-tool-batch-snapshot` (commit bf34a31) — auto-merged engine.py and test_loop_engine_p0_fixes.py via ort strategy, no conflicts. 494 passed.
- `git merge --no-ff fix/runtime-skill-extraction` (commit 07ff20b) — auto-merged cleanly including the `git mv` rename. 497 passed.

Final state: main is 9 commits ahead of origin (3 refactor commits + 2 merge commits + 2 supporting commits from funnel branch + 2 from previous main work). 497 passed, 1 skipped.

Encountered: `test_memory_tiers.py::test_append_journal_no_duplicate_header_under_concurrency` flaked twice during the test runs (passed when run in isolation, failed once intermixed with the full suite). Pre-existing flake unrelated to the refactors. Worth a separate investigation pass.

Updated `topics/runtime-review-2026-04-25.md` with ✅ DONE markers for #2 and #3 + a "Deferred follow-ups" section listing the 9 remaining smaller findings (Codex Part 1 #5 #6, Part 2 #3 #5 #8 #10, Part 3 #4 #6 #7 #9). These are all small independent items — could be cleaned up in a single follow-up "Codex review tail" PR.

Doc-update commit pending; will commit topic file + journal in a single docs(memory) commit. Branches not yet deleted in case rollback is needed.

## 2026-04-25 22:54 [ship]
Shipped all 5 highest-leverage runtime-review followups (combined opinion review session). Branch fix/runtime-review-followups, worktree, fast-forward merged to main.

Commits (oldest → newest):
1. 80315bb — checkpoint fsync (Codex Part 2 #3) — temp-file fsync + best-effort parent-dir fsync; Windows-safe (try/except OSError on dir open). +regression test (os.fsync spy).
2. 662fde1 — _MODEL_PRICING RLock (Codex Part 1 #6) — defensive against PEP-703 free-threaded CPython + concurrent MCP registrations. CPython 3.14 GIL makes the canonical race nearly unreproducible; lock is uncontended on hot path. +regression test (no-deadlock + visibility).
3. a733f8c — verification.should_verify(since_index=) (Codex Part 2 #5) — was scanning entire history, replayed historical mutations re-armed gate on non-mutating turns. Engine captures len(messages) at turn start. +2 regression tests proving since_index excludes/includes correctly.
4. 3044a57 — LLMOptions/ModelSpec moved into jyagent.runtime.loop.llm_types (Codex Part 3 #5 second half). Closes runtime→llm dep direction completely. jyagent.llm.types re-exports for backward compat — class identity preserved across all 4 import paths. +regression test (subprocess-isolated to avoid sys.modules pollution mid-suite — earlier in-process version broke 23 tests via cached module references).
5. f360cda — Cost unification (Codex Part 1 #9 #10) — extracted pure compute_call_cost(usage, provider, model) → CostBreakdown in stats.py. Engine _CostTracker and SessionStats._record_cost both delegate. Pre-fix engine ignored long_context_*_multiplier and input_tokens_include_cache_reads — budget under-counted by 2× on Anthropic 1M-context calls. +6 regression tests proving identical math across both trackers.

Diff: 13 files, +764/-96. Tests: 490 passed (was 477 baseline + 13 new).

Process notes:
* Worktree gotcha bit me again at first — needed `uv sync --all-extras` (NOT just `uv sync`) inside worktree to get pytest. The first `uv run pytest` failed with "no module named pytest" until then.
* Codex second-opinion review (run in parallel): network-died at 71k tokens before synthesis, third instance of the documented pattern. Got 1 useful early observation ("LLMClient is a clean behavioral extraction at the type level, but the engine still imports concrete value types from jyagent.llm") which validated the plan for fix #5.
* Subprocess-isolation lesson: a test that calls `del sys.modules[key]` for already-imported jyagent.* modules silently breaks any later test that holds a top-level `from jyagent.X import foo` reference — `mock.patch("jyagent.X.foo", ...)` then patches a different module than the test sees. 23 false failures in test_skill_router/test_subagent/test_tracing_and_verification surfaced this. Fix: subprocess.run for any in-test sys.modules surgery.

Remaining open items from the 2026-04-25 review (now lower-priority):
* Part 1 #1, #2, #3 — daemon-thread tool-body design (acknowledged trade-off, well-documented)
* Part 1 #5 — SessionStats provider/model property reads bypass lock (CPython-atomic for str reads; benign)
* Part 1 #7, #8 — ToolResult string-content typing (cosmetic; not behavioural)
* Part 1 #11 (live-read accessors on ToolRegistry kept as deprecated-but-present footguns — should mark @deprecated)
* Part 2 #1 phase policy collapse, #4 write_todos no merge guard, #8 checkpoint replay state loss (all v1 design choices)
* Part 2 #9 on_assistant_message tool_call_id pairing (open — caller responsibility, document loudly in callbacks.py)
* Part 2 #10 max-step fallback tool_choice="none" (open — provider-dependent, hard to reproduce)
* Part 3 #1 runtime/__init__.py eagerly loads engine (needs PEP-562 lazy __getattr__)
* Part 3 #2 module-level _tool_dispatch_executor + atexit.register (cosmetic)
* My fresh observations (1) engine.py size, (8) skills.py 28KB single file — defer until next feature triggers them.

Combined verdict from the review session is unchanged: direction of travel is good, code quality (docstring density + citing past reviews in source) is unusually high. After this batch, ~15 of the original 34 Codex findings are closed.

## 2026-04-25 23:26 [ship]
Shipped feat/memory-upgrades → main (commit 1e3f95e). Five capabilities: BM25 search across topic+journal (search.py), reconciliation in extraction with ADD/UPDATE/NOOP, non-destructive supersede() with strikethrough audit trail, reflection pass at compaction emitting [reflection] candidates to journal, section-level topic reads.

Sub-agent code review (Codex network was flaky — fell back to dispatch_agent) caught 3 CRITICAL bugs and 3 HIGH security issues, all fixed before merge:
- C1: append_memory_md missing-newline corruption
- C2: race conditions on MEMORY.md → added _MEMORY_MD_LOCK reentrant lock
- C3: _apply_directive falsely counted no-match UPDATEs as success
- H1: path traversal via topic name → _topic_path() validator
- H2: UPDATE could strike through headings + Behavioral Rules → protected sections
- H3: insufficient post-LLM validation → sanitize body (1 line, 10-120 chars, no # or ~~)

Tests: 113 passed in memory+compaction subset; 502 in wider package (web_search/chrome_concurrency intentionally skipped, pre-existing failures on main). Used git worktree per the documented gotcha (uv sync inside worktree to get a real local editable install). Topic memory-design.md updated with migration history + new API surface.
## 2026-04-26 00:11 [ship]
2026-04-25 — Codex review of jyagent.runtime → Tier-A fixes shipped to branch `runtime/codex-review-fixes-2026-04-25`.

Reproduction: 3 codex exec runs of the runtime review.
- Run 1 (default): failed @181s — `crs.us.bestony.com` proxy SSE idle-kill during long silent reasoning phase. 5 reconnects, all fatal. 100K tokens lost.
- Run 2 (`-c model_reasoning_summary=auto`): SURVIVED @275s. Summary deltas kept SSE alive.
- Run 3 (config persist): failed @225s. Proved fix is probabilistic, not deterministic.

Conclusion on Codex flakiness: `model_reasoning_summary=auto` shifts the disconnect distribution toward success but doesn't cure. Real fix is provider-level (the `neo` proxy is fundamentally unreliable for long reasoning jobs). Config patched anyway since it helps.

Codex's report: 10 issues found, all verified by spot-checking actual code.

Tier A shipped (2 commits on the branch):
- b11dd3a: A2 (lazy/grow tool dispatch executor honouring max_tool_workers), A3 (try/except around trace finalize, route to module logger), A4 (regex-based run_id sanitisation blocking `..` traversal). +12 tests.
- e1dfcaa: A1 (mutating tool metadata + LoopResult.partial_side_effects accumulator + WARNING log on timeout). Delegated to Claude Code (ran 25min, hit my hard timeout mid-summary but had completed all edits + tests). +4 tests.

Test deltas: 529 → 541 (+12 from A2/A3/A4) → 545 (+4 from A1). Zero regressions.
Pre-existing failure in test_web_search.py::test_first_engine_wins remains (cascade-order; unrelated).

Branch sits ahead of main, NOT pushed/merged. Memory updates on main were stashed (`git stash@{0}: wip-memory-updates`).

Open work:
- Tier B (B1 max-steps fallback bypasses cost tracking; B2 ToolBatch dict mutability; B3 run_shell timeout coercion) — small but separate PR
- Tier C (C1 cancellation latency; C4 engine.py 5-component split) — design work, see `topics/runtime-refactor.md`
- Subprocess hard-kill for shell-class mutating tools — companion to A1, future PR

## 2026-04-26 00:37 [ship]
2026-04-25 (cont.) — Tier B + Tier C (partial) shipped on `runtime/codex-review-fixes-2026-04-25`.

Tier B (commit b3f71ed):
- B1: max-steps fallback now records cost (was systematically under-counting trace_total_cost_usd by the fallback turn).
- B2: ToolBatch dict fields wrapped in MappingProxyType — schema_map/functions/timeout_hints/large_input_keys/compaction_priority now read-only views via small _readonly() helper. Mutation raises TypeError loudly.
- B3: run_shell timeout coercion (int(user_timeout)) wrapped in try/except (TypeError, ValueError) — hallucinated "30s" or list-typed timeouts no longer crash the step.
- +11 tests. 545 → 556 passed.

Tier C (commit 74097ba) — shipped what was sane:
- C2: AgentLoop reentrance guard via threading.Lock (non-reentrant). Lazy-init in run() so legacy AgentLoop.__new__-based test fixtures keep working. Released in finally so exceptions don't deadlock next run.
- C3: SessionStats provider/model property reads now take self._lock — matches set_active_model writer contract; correct under free-threaded CPython.
- +6 tests. 556 → 562 passed.

Tier C deferred (intentionally, design note in topics/runtime-c1-c4-deferrals.md):
- C1: cancellation latency in blocking provider calls — needs LLMClient.cancel() protocol + watcher thread that closes httpx connection on cancel_event. ~1 day.
- C4: split engine.py (still 2113 lines) into 5 components. ~3 days as 5 PRs. Out of scope this session.

Total branch state: 4 commits, 562 passing tests (was 529 baseline; +33 net new tests across A1/A2/A3/A4/B1/B2/B3/C2/C3).

Pre-existing failure in test_web_search.py::test_first_engine_wins still ignored. Flaky test_memory_tiers.py::test_append_journal_no_duplicate_header_under_concurrency observed once (passes on rerun).

Branch is local-only, NOT pushed/merged. data/memory/ work on main still stashed (`stash@{0}: wip-memory-updates`).

## 2026-04-26 10:14 [ship]
2026-04-25 (cont.) — Tier C C1 + C4 Phase 1 shipped on `runtime/codex-review-fixes-2026-04-25`.

**C1 — cancellation latency in blocking LLM calls** (commit 85e7719)
- `_call_complete`: pre-call guard + daemon worker + 100 ms main-thread poll. Cancel latency now <1.5 s end-to-end (was up to provider HTTP timeout 60-300 s).
- `_call_streaming`: daemon watcher on cancel_event that calls `stream.close()` when fired (50 ms polling). Closing httpx response unblocks network-stuck iterator. Cancel latency <0.5 s.
- Fast path preserved on both when cancel_event is None — zero overhead.
- Daemon worker leak accepted (Python threads not cancellable, same trade-off as mutating-tool timeout).
- +4 tests in TestC1CancellationLatency. 562 → 566 passed.

**C4 Phase 1 — extract _CostTracker → runtime/loop/cost.py** (commit 4e7b5d5)
- First of 5 phases in the engine.py split plan. Deliberately the cheapest extraction to validate the pattern before bigger moves.
- New file jyagent/runtime/loop/cost.py with class CostTracker (renamed from _CostTracker).
- engine.py replaces inline with: `from .cost import CostTracker as _CostTracker` — every internal call site continues to work unchanged.
- +4 tests in TestC4Phase1CostExtraction (both import paths, priced/unpriced recording). 566 → 570 passed.
- Pattern verified: extract-file + alias + targeted tests, 0 behavior change.

**C4 Phases 2-5 DEFERRED** (each is its own PR):
- Phase 2: tool executor helpers → tool_executor.py (~300 lines, ½ day, medium risk)
- Phase 3: LLM call + retry/fallback → llm_runner.py (~400 lines, 1 day, HIGH risk — most tangled module, lots of closure state)
- Phase 4: compaction → compaction.py (~200 lines, ½ day, low risk — pure functions)
- Phase 5: what remains = LoopController (~600-700 lines, 1 day, medium risk — every _run_impl exit path needs _finalize_run invariants preserved)

Full details in `data/memory/topics/runtime-c1-c4-deferrals.md`. Recommended: Claude Code delegation per phase from that plan.

**Final branch state** (6 commits, 570 passing tests, +41 net new tests from 529 baseline):
```
4e7b5d5 C4 Phase 1 — extract _CostTracker
85e7719 C1 — cancellation latency
74097ba C2/C3 — reentrance guard + SessionStats locking
b3f71ed B1/B2/B3 — cost tracking, immutability, timeout coercion
e1dfcaa A1 — mutating timeout → LoopResult
b11dd3a A2/A3/A4 — dispatch executor, tracing non-fatal, path sanitisation
```
Local-only, not pushed/merged. `data/memory/` work on main still stashed (`stash@{0}`).

## 2026-04-26 11:23 [ship]
2026-04-25 (cont. 4) — C4 Phase 2 shipped on `runtime/codex-review-fixes-2026-04-25` (commit 13fa6b2). Tool executor stack moved to runtime/loop/tool_executor.py.

Extraction:
- get_tool_dispatch_executor, execute_tool, execute_tool_with_timeout, execute_tools
- Module globals: tool_dispatch_executor, tool_dispatch_lock, tool_dispatch_cap
- engine.py: 2226 → 1862 lines (-364, ~16% smaller)
- Kept in engine.py: _is_transient_error (that's Phase 3's retry concern)

Back-compat: function aliases + PEP-562 __getattr__ passthrough in engine.py for mutable module state. Critical because the pool is rebound on grow — plain `from X import Y` would snapshot stale.

Delivery split: ~80% Claude Code (died mid-task at 23 min with HTTP 424 peer reset after landing the file + engine aliases), ~20% jy-agent finished (PEP-562 shim + 6 new tests + touch-up of one existing test that imported via value-copy at module-load and went stale after any grow).

Tests: 570 → 576 passed (+6 in TestC4Phase2ToolExecutorExtraction). 0 regressions after the one touch-up.

Final branch state: 7 commits, 576 passing tests (+47 from 529 baseline).

**New lesson learned**: When delegating to Claude Code for medium+ tasks, always plan for ~20% salvage work. The --bare mode dies silently on API blips but leaves partial work on disk. Best practice: grep for the key deliverables (__getattr__ was missing, tests missing) before committing; run the test suite to see what's actually broken.

**Separate lesson**: For extractions involving rebindable module state (pools, caches, singletons), the PEP-562 __getattr__ passthrough is essential. A plain alias goes stale the moment the state is rebound in the new home. Save/restore patterns in existing tests must also target the canonical home, not a static attribute on the re-exporter (which would shadow __getattr__).

Phase 3 (llm_runner.py) deferred — HIGH risk due to closure state, recommend routing to stable provider with ≥2400s timeout. Phases 4-5 also deferred per `topics/runtime-c1-c4-deferrals.md`.

## 2026-04-26 14:27 [session]
C4 Phase 3 (llm_runner extraction) — resumed + shipped

Previous session died at step ~28 with `jy-agent Killed: 9` (likely OOM in agent loop — file was 1.7k lines of repeated context on engine.py). Recovered cleanly by:

1. `git status` on worktree → 4 files half-done (llm_runner.py created, engine.py partially edited, 3 method bodies NOT yet delegated).
2. Read the 3 methods in engine.py + confirmed LLMRunner's public API in llm_runner.py.
3. Replaced L1435-L1754 (320 lines) with thin delegates via Python heredoc.
4. Imports + `uv run python -c "..."` smoke test passed.
5. Full test suite → 1 expected failure (structural test inspecting `_call_streaming` source, which is now a one-liner).
6. Fixed that test to inspect `LLMRunner.call_streaming` instead.
7. Re-ran → 5 failures: 4 web_search (pre-existing on main per memory note re: DDG cascade reorder) + 1 real regression: `test_cancel_interrupts` got `status='error'` instead of `'interrupted'`.

**The one real Phase 3 landmine** was subclass-override contract:
- Test subclasses `AgentLoop` and overrides `_call_streaming` / `_call_complete` to raise transient errors, then calls `_call_llm_with_retry` expecting its overrides to drive the retry.
- My first cut routed `_call_llm_with_retry` straight to `LLMRunner.call_with_retry` — which calls `self.call_streaming` on LLMRunner, NOT on the AgentLoop subclass. Overrides bypassed.
- Fix: kept the retry loop inline in AgentLoop (~50 lines), using `self._call_streaming` / `self._call_complete` dispatch. `LLMRunner.call_with_retry` still exists as a public API for future direct use but AgentLoop doesn't route through it.

Also: `test_subagent::test_cancel_interrupts` patched `jyagent.runtime.loop.engine.get_reasoning_config_for_provider` — patch target moved with `_build_runtime_options` to `jyagent.runtime.loop.llm_runner`. Updated test.

Added 7 tests in `TestC4Phase3LLMRunnerExtraction`:
- import-path identity for the 4 back-compat aliases
- LLMRunner class/method surface
- `_get_llm_runner()` cache behaviour (same instance across calls)
- `_call_complete` / `_call_streaming` delegate forwarding (patch runner, confirm via engine entry point)
- `_call_llm_with_retry` preserves subclass overrides (critical: this is the test that would have failed my first attempt)

Final: engine.py 1862 → 1568 lines (-294 this phase, -658 across Phases 1-3). llm_runner.py: 567 lines. 583 passed (non-web_search), 0 regressions. Commit `7785c1e` on branch `runtime/codex-review-fixes-2026-04-25` (8 commits, local only, not pushed).

Lesson for future extractions: add "audit subclass/monkeypatch override contract" as a named step before writing the replacement — don't rely on test coverage to catch it mid-implementation.

Also worth keeping the crash recovery pattern: when jy-agent dies mid-task, the first three moves are always (a) `git status` on the worktree, (b) `git log --oneline` to see what's committed, (c) read the topic file to see the plan. Memory + git = full recovery context, 3 tool calls.

## 2026-04-26 15:27 [session]
C4 Phase 4 shipped + Phase 5 re-scoping decision

Phase 4 (compaction.py extraction) was the textbook-easy phase:
- Moved 3 pure functions (truncate_result, compact_messages, truncate_tool_call_blocks) into new compaction.py (245 lines).
- Engine keeps 3 underscore-prefixed back-compat aliases via `from .compaction import X as _X`.
- No PEP-562 shim needed (nothing mutable, unlike Phase 2's tool_executor state).
- +5 tests in TestC4Phase4CompactionExtraction (identity, head/tail split, error-passthrough, fast-path identity, no-op on unknown tools).
- 583 → 588 passed, 0 regressions.
- ~30 min wall-clock, exactly as "low risk" predicted.

Commit: ef970e5. Branch now has 9 commits, engine.py down to 1394 lines (2226 → 1394, -37% across Phases 1-4).

**Phase 5 needs re-scoping — original plan is off by 2×.**

Phase 5 was spec'd as "engine.py becomes LoopController, ~600-700 lines, medium risk". Reality: engine.py is 1394 lines and **_run_impl alone is ~733 lines** (L539-L1271), which Codex's original plan never addressed. The four extracted modules (cost, tool_executor, llm_runner, compaction) each have a clean single responsibility; _run_impl is the remaining orchestrator.

Two options I've written into the topic file:
- **Option A**: Accept current state. Rename AgentLoop → LoopController as pure symbol change (~20 import sites to update). Low value.
- **Option B**: Extract _run_impl step-body into step.py as `run_step(state, ...) -> StepResult`. 1-day medium-to-high-risk refactor. High value — _run_impl becomes unit-testable without provider mocking.

Recommendation I gave user: ship Phases 1-4 as the C4 delivery, close out the branch, decide A vs B as a separate project. The current branch delivers the bulk of the structural value Codex called for and is ready to merge on its own merits.

Lesson: extraction-plan estimates based on "what's left after extraction N" are fragile when one method in the remainder dominates. Should have counted `_run_impl` size up-front (wc -l on the def) rather than assuming remainder size correlates with orchestration complexity.

## 2026-04-26 16:21 [ship]
C4 Phases 1-4 + Codex Tier A/B/C1 — merged to main, branch closed

Closed out the `runtime/codex-review-fixes-2026-04-25` branch per recommendation. Final state:

**Merge commit**: `0e21277` — `--no-ff` merge into main, 9 feature commits preserved as branch line in graph.
**Docs**: `799a187` (C4 tracking topic file + session journal) committed before merge; `ae6d29b` popped the pre-existing `wip-memory-updates` stash (2026-04-25 evening notes that pre-dated the C4 effort) and resolved a journal append-vs-append conflict by ordering chronologically.
**Worktree**: `/Users/jyxc-dz-0100398/jy-agent-runtime-fix` removed.
**Branch**: `runtime/codex-review-fixes-2026-04-25` deleted (was at ef970e5, fully merged).
**Remaining stash**: `stash@{1}: WIP on main: 49bfeed fix(web_search):...` left untouched — unrelated to C4, predates branch.

**Not pushed to origin.** Main is 19 commits ahead of origin/main (this batch + prior unpushed work from the 2026-04-25 ship sessions). Pushing is a larger-scope decision than just the C4 work; left to the user.

Test status on merged main: 588 passed, 0 regressions (non-web_search). 4 web_search failures are pre-existing — DDG cascade reorder, tracked separately.

Aggregate impact across the 9 C4-branch commits:
- engine.py: 2226 → 1394 lines (-832, -37%)
- New modules: cost.py (77), tool_executor.py (449), llm_runner.py (567), compaction.py (245)
- +59 net new tests (529 baseline → 588 passing)
- Tier A: 4 fixes (A1-A4 — mutating timeout, dispatch executor, tracing, path sanitisation)
- Tier B: 3 fixes (B1-B3 — cost tracking, immutability, timeout coercion)
- Tier C: 3 fixes (C1 cancellation, C2 reentrance, C3 stats locking)
- C4: 4 phases shipped, Phase 5 (rename or step.py extraction) re-scoped and deferred

Phase 5 documented in `topics/runtime-c1-c4-deferrals.md` with two options (Option A: pure rename, low value; Option B: extract `_run_impl` into step.py, high value but needs its own project scoping). User to decide as a separate effort if desired.

Process notes for future similar work:
- The stash-pop conflict was a clean append-vs-append (both sides added blocks to the same anchor line). Python script resolution beat manual editing — counted markers, sliced, ordered chronologically. ~10 lines of code.
- `git worktree remove` cleanly + `git branch -d` (merged) is the canonical close-out sequence. Don't `-D` (force) — the safe `-d` confirms the branch was actually merged.
- Pre-existing `stash@{1}` predates the work and is out-of-scope. Leaving it lets the user decide its fate without me making that call.

## 2026-04-26 17:55 [ship]
2026-04-26 17:55 — pushed 20 commits to origin/main + fixed the 4 stale web_search tests

**Push**: 19 unpushed commits (C4 Phases 1-4 + Codex Tier A/B/C1 + memory upgrades + earlier runtime fixes) plus the 16:21 ship-note journal commit. `git push origin main` clean. b9ec70d..7194943.

**web_search test fix**: The 4 pre-existing failures (`TestCascade::test_first_engine_wins`, `test_falls_through_to_next_when_underfilled`, `test_engine_exception_recorded_and_continues`, `TestWebSearch::test_returns_formatted_results`) were stale — they assumed DDG was first in the cascade, but DDG was moved to LAST on 2026-04-25 due to flakiness. Source order is now `searxng → brave → mojeek → ddg`.

Each test was rewritten to exercise the same SCENARIO against the new first engine (brave):
- first-engine-wins: brave wins, mojeek/ddg fail-if-called
- fall-through: brave returns [], mojeek wins, ddg fail-if-called, error mentions "brave"
- exception-recorded: brave throws "kaboom", mojeek wins, error has "brave" + "kaboom"
- formatted-output: patch brave, assert "Engine: brave" in formatted text

Spirit preserved (test purpose unchanged); only the engine name in the assertions moved to match the new cascade head. test_force_override_via_env was already order-independent and still passes.

Result: full suite 608 passed, 1 skipped, 0 regressions (was 588 + 4 fail). Commit 7194943, pushed.

MEMORY.md updated: replaced the "DDG flakiest, moved last" tip with a clearer cascade-order reference that mentions the test-file invariant. The old phrasing was a debugging hint; new phrasing is a durable rule.

Lesson: when reordering a cascade/priority list in source, grep tests for the old engine names — order-dependent assertions don't fail at collection time, only at run time, and "pre-existing failures" can hide real test-source drift for weeks.

## 2026-04-26 20:27 [ship]
2026-04-26 20:25 — C4 Phase 5 shipped (3 commits, branch runtime/c4-phase5-step-extraction)

Codex was unavailable today — five attempts across both `neo` (403 Forbidden) and `models-proxy` (high-demand 5x reconnect) endpoints, including one final `codex review --base main` attempt against the actual diff. User authorised proceeding without the second-opinion pass. Solo design + implementation.

**Shipped (3 commits, all passing 623 tests, 0 regressions):**
- `08bd56a` C1 — extract per-step body to step.py (470 lines) as run_step(loop, state) -> StepOutcome. Engine retains for-loop counter + post-loop terminal handlers + try/except. Setup phase still inline in _run_impl.
- `83b214c` C2 — extract setup phase into RunState.from_loop() classmethod. Engine _run_impl shrinks to ~30 lines of orchestration. Side effects on loop (_partial_side_effects reset, _run_id init, _todos seed) live in from_loop intentionally — they're per-run setup, not init-time.
- `dfc7f11` C3 — tests/test_step_runner.py with 15 step-level unit tests against a ~50-line FakeLoop. Demonstrates the testability win: each test is ~10 lines of dataclass construction instead of full provider mock chain.

**Aggregate impact:**
- engine.py: 1394 → 866 lines (-528, -38%). Total since pre-C4: 2226 → 866 (-1360, -61%).
- step.py: 796 lines new module (470 of body + 116 of from_loop + 130 of dataclasses + 80 of doc).
- All loop/ modules: cost(77) + tool_executor(449) + llm_runner(567) + compaction(245) + step(796) = 2134 lines, all single-responsibility.
- Tests: 608 → 623 (+15), 0 regressions.

**Design decisions vindicated by implementation:**
- D1 (cut at per-step body only): correct. Post-loop max_steps fallback in engine reads `state.last_step_batch` cleanly. The fallback's no-tools LLM call would have been awkward to thread through step.py.
- D2 (mutable RunState passed by reference): correct. cost_tracker / stuck_detector / tools_batch identity preserved across iterations because Python passes object refs by default. Existing E2E tests (cost budget, dedup_break) pass without change.
- D3 (StepOutcome tagged union): correct. StepBreak vs StepTerminate distinction matters — StepBreak runs the engine's cancelled-exit handler which finalizes with `state.step + 1` based on the LAST step actually attempted. StepTerminate carries an already-finalized LoopResult with its own step count.
- D4 (subclass-override contract): preserved automatically. test_subclass_override_of_call_llm_is_used confirms.
- D5 (_partial_side_effects and _todos stay on loop): correct. tool_executor reaches for loop._partial_side_effects via the partial_side_effects= kwarg engine threads; write_todos closure reads loop._todos via accessor functions.

**Test fixes that landed in C1+C2 (5 structural tests):**
- _loop_combined_source() helper added to test_loop_engine_p0_fixes.py: concatenates `inspect.getsource(_run_impl) + inspect.getsource(run_step)` for tests whose grep target moved to step.py.
- 5 tests in test_loop_engine_p0_fixes.py: stuck_detector.record, seen_batch_keys, step+1<cfg.max_steps, _is_truncated, no-bare-LoopResult-returns — all switched to combined source.
- test_phases TestRunImplWiresPhasePolicy: switched to step.run_step source. Also dropped `self._` prefix from assertion strings (run_step calls go through `loop._fire`, not `self._fire`).
- test_reflection: split lazy-import check (from_loop source) from consumption check (run_step source). C2 also moved the lazy import target.
- test_cost_record_uses_effective_spec_variable: split into 3 source checks (from_loop / run_step / _run_impl-fallback) — each piece checked at its actual location.

**Two unit-test fixture issues caught and fixed in C3:**
- ModelSpec(model="claude-test") triggers Anthropic adaptive-reasoning validation; switched to "claude-sonnet-4-6" (per error-message guidance). Real implementation lesson: build_runtime_options is NOT a pure construction — it consults model-name validation. Future tests should use real model names or mock the validation.
- TodoItem isn't dict-subscriptable (normalize_todo returns a dataclass). Fixed test to use `.content` attribute access.

**Branch state (NOT pushed):**
- 4 commits ahead of main: 68263f8 (docs), 08bd56a (C1), 83b214c (C2), dfc7f11 (C3).
- 0 outstanding test failures, 0 lint issues caught (no lint run yet).
- Codex review still pending — user instructed proceed without it; could retry later.

**What I'd want a Codex review to scrutinize (still pending):**
1. Engine's post-loop max_steps fallback at `state.last_step_batch` — relies on run_step having set this on the LAST iteration before the for-loop exits via "fall through" (max_steps reached normally). If the last step was a StepBreak (cancel) or StepTerminate (early return), state.last_step_batch is still the value from that iteration — correct. But if max_steps=0, the loop never runs, last_step_batch is the default ToolBatch.empty() — and the fallback would be triggered with no tools_batch context. Today this is fine because fallback removes tools from context. Worth a Codex eye on the boundary condition.
2. RunState.from_loop's intentional side effects on loop — clean documented, but a reviewer might prefer separating "setup loop" from "build state" into two phases.
3. The aliasing `trace = state.trace; cost_tracker = state.cost_tracker; effective_spec = state.effective_spec` after from_loop — kept for diff minimality but slightly cargo-cult. Could be inlined as state.X reads in the post-loop fallback.

**Lessons for solo Codex-substitute work:**
- Writing a design doc BEFORE implementation paid off — every decision had a clear answer when implementation hit it (e.g., "should _partial_side_effects move to RunState? — D5 says no, keep on loop"). The doc was the deliberation, the commits were execution.
- Test transcription errors caught structurally: 5 tests greppped patterns that moved; helper-function approach (_loop_combined_source) was cleaner than per-test surgery.
- Fixture issues only show up at runtime: the fake-model-name reasoning-validation breakage and the TodoItem subscript both required actually running the test. Worth a "smoke test" pass on every new test fixture before claiming done.

## 2026-04-26 20:43 [ship]
2026-04-26 20:35 — C4 Phase 5 merged + pushed, branch closed

`git merge --no-ff runtime/c4-phase5-step-extraction` into main (merge commit 8908fc8), then `git push origin main` clean (7194943..8908fc8). Branch deleted (`-d`, safe-merged check). Working tree clean.

Test status on merged main: 623 passed, 1 skipped, 0 regressions. Confirmed end-to-end before push.

C4 effort is now COMPLETE end-to-end:
- Phases 1-4 shipped previously (cost.py, tool_executor.py, llm_runner.py, compaction.py)
- Phase 5 (step.py + RunState.from_loop()) shipped today
- engine.py: 2226 → 866 lines across all 5 phases (-1360, -61%)
- 5 new owned modules under runtime/loop/: cost(77) + tool_executor(449) + llm_runner(567) + compaction(245) + step(796) = 2134 lines, all single-responsibility
- Tests: 529 (pre-C4) → 623 (post-Phase-5), +94 net new tests, 0 regressions

Codex review for Phase 5 still outstanding — provider was unavailable across all 5 attempts today. Could be retried later if a fresh eye is wanted on:
1. state.last_step_batch boundary at max_steps=0
2. RunState.from_loop's intentional side effects on loop
3. The trace/cost_tracker/effective_spec alias triplet in engine post-from_loop

None of these blocked the merge — they're code-style observations not behavioural concerns. User accepted (A) merge-as-is per the design doc + passing tests.

Branch closeout pattern (now the standard for jy-agent runtime work):
- Per-phase commits on a feature branch
- Topic file in data/memory/topics/ captures pre-implementation design
- Per-session journal entries capture progress + lessons
- `git merge --no-ff` to keep branch line visible in graph
- `git branch -d` (safe, not -D) confirms the branch was actually merged
- Push immediately after merge
- Clean working tree before declaring done

## 2026-04-27 13:38 [ship]
2026-04-27 13:30 — C4 Phase 5 Codex review pass + 3 follow-up commits shipped

**Codex review (the deferred one from yesterday):** finally landed clean today after Codex was restored. Three attempts:
1. `codex review --commit 8908fc8`: 403 on `neo`, "high demand" on `models-proxy`. Failed.
2. `codex exec` full-fat review: 77k tokens of investigation INCLUDING two empirical probe tests on `max_steps=0` boundary — got the answer (SAFE in all three sub-cases: fallback off, fallback on, cancel pre-set) but stream dropped before final markdown.
3. `codex exec` tight per-question review (skipping Focus 1 since already empirically validated): 145s, succeeded cleanly, ~30k tokens.

**Verdict on the three implementer-flagged areas** (all APPROVE-WITH-NITS):
- A. `RunState.from_loop` naming → RENAME-ONLY (Codex disagreed with my "minor nit" — said the name actively reads as pure factory)
- B. trace/cost_tracker/effective_spec aliases → trace KEEP, cost_tracker KEEP (Codex counted 7 use sites; I'd missed L692), effective_spec INLINE (only 2 uses)
- C. StepOutcome dispatch fall-through → ADD-ASSERT (Codex agreed)
- D. StepOutcome contract: ALL 12 returns valid (Codex audited)
- E. test_step_runner.py coverage gaps: MATERIAL — 17 enumerated zero-coverage paths in step.py (tool execution, dedup, reflection, checkpoint, …)
- F. Subclass overrides: LOW RISK except — **new finding**: tool execution is NOT an instance override point in step.py (calls `tool_executor.execute_tools` directly). Old patches against `engine._execute_tools` silently lose effect against the per-step path.

**Three follow-ups shipped (commit d83ff5e, merged d1eccdd, pushed):**
1. `RunState.from_loop` → `RunState.prepare_for_run` (21 references across step.py, engine.py, 3 test files)
2. Inlined `effective_spec` alias at single use site (engine.py:L657-L658 now `state.effective_spec.{provider,model}`)
3. `assert isinstance(outcome, StepContinue)` after the StepBreak branch in `_run_impl`

5 files changed, +46/-32. 603 tests pass (excluding test_web_search), 0 regressions. Pushed to origin/main as d1eccdd.

**Deferred (4 + 5 from the review):**
- D4 (~30 min): document tool-executor call-path change in engine.py:L81 alias comment + add a memory gotcha
- D5 (~half day): "C5 — fill step.py tool-path test gaps" — ~10 unit tests for L633-L796 region

**Process lessons captured:**
- Codex provider flapping is real and systemic; tighter prompts succeed when wide ones fail. Strategy: scope down per-question rather than retrying the same wide review prompt.
- The empirical probe-test approach Codex took on Focus 1 (running `_run_impl` with `max_steps=0` against a constructed AgentLoop) was genuinely valuable — it answered the boundary question with hard evidence rather than reasoning. Worth replicating for similar boundary questions in future reviews.
- Codex caught a subclass-override risk I'd missed (tool_executor direct import). Confirms the "Codex as second opinion" rule is paying off — would have shipped without this finding otherwise.

**Branch hygiene:**
- Branch `runtime/c4-phase5-codex-followups` deleted after safe merge.
- Stashed `wip-memory-gotcha` (uncommitted MEMORY.md `git status` gotcha) was popped after merge — still uncommitted on main, will land in next memory-docs commit.

## 2026-04-27 15:00 [ship]
2026-04-27 14:30 — C4 Phase 5 D4 + C5 shipped (doc + 12 step.py tests)

After yesterday's Codex follow-up commit (rename/inline/assert), tackled the two deferred items in one branch.

**D4 — engine.py:L74-L93 doc update.** Documented the back-compat scope of the `engine._execute_tools` alias inline. Key insight from Codex's Focus F finding: after Phase 5, `step.py::run_step` calls `tool_executor.execute_tools` *directly*, not through the engine alias. So downstream tests/extensions that monkeypatch `jyagent.runtime.loop.engine._execute_tools` silently lose effect on the per-step path. Patch `jyagent.runtime.loop.tool_executor` instead.

Also added a durable [gotcha] to MEMORY.md capturing this rule.

**C5 — 12 new step.py unit tests.** Added in `tests/test_step_runner.py` covering the largest coverage gaps Codex flagged:
- `TestNormalToolExecution` (3): round-trip, on_tool_start/end pairing invariant, error-result flag
- `TestCancellationBeforeTools` (1): staged cancel between on_tool_start and execute_tools — verifies on_tool_end still fires (UI spinner-leak prevention)
- `TestCancellationAfterTools` (1): cancel between tool exec and reflection
- `TestStuckLoopDedupBreak` (1): three identical (tool, args, result) → StepTerminate(dedup_break)
- `TestDuplicateToolCallsInSingleBatch` (1): three identical calls in ONE batch count as one (Codex Part 2 #4 fix)
- `TestCheckpointWrite` (3): cadence boundary fires; dir=None skips; off-cadence skips
- `TestTodosOverlayInStepBatch` (2): write_todos overlaid/absent

Mock pattern: `mock.patch("jyagent.runtime.loop.tool_executor.execute_tools", return_value=...)`. Fast — all 12 tests run in 0.57s wall against a `FakeLoop` with no provider/runtime/registry state.

**Test counts:**
- test_step_runner.py: 15 → 27 (+12, +80%)
- Full suite: 603 → 615 (+12), 0 regressions
- Both runs ~38s

**Commits:**
- 47b3cf3 — runtime: C4 Phase 5 follow-ups (doc + step.py test coverage)
- cb0aa01 — Merge branch 'runtime/c4-phase5-doc-and-tests'
- Pushed to origin/main. Branch deleted (safe-merged).

**Coverage paths STILL uncovered (deferred):**
- Reflection injection (step.py:L757)
- Compaction path (L391-L405)
- Phase policy (L434-L460)
- Verification gate (L539)
- Trace-enabled spans (L473, L678)
- Unpriced-cost warning (L491)

Most are E2E-covered elsewhere. A future "C5b" could fill these but ROI is lower than the 12 we just shipped — those covered the run_step paths most likely to break silently in future refactors.

**Lesson:** mocking `tool_executor.execute_tools` (instead of trying to mock the registry) is the right abstraction layer for step-level tests — it's exactly the boundary `run_step` reaches across, and the mock pattern is 1 line vs ~30 lines of registry fixture. Worth remembering for any future runtime/loop unit tests.

**Process note:** the edit_file tool truncated my initial test-file append at ~14750 chars (writeable cap) — but pytest still passed because the truncation happened at a class boundary, not mid-method. Got lucky. Should always run pytest immediately after a large append to catch truncation early.

## 2026-04-27 17:55 [ship]
2026-04-27 17:00 — P3-1 + P3-2 import-time cleanup shipped (commits 0c9f77b, ee991d8, merged eeaa5e1, pushed)

Picked B from the day's morning menu but corrected an honest error: I'd listed C1 as open; it shipped 2026-04-25 (`85e7719`). The actual remaining Tier-3 items from the Codex review were P3-1 (eager engine load on `import jyagent.runtime`) and P3-2 (module-level dispatch pool + atexit at import). Both naturally bundle as a "runtime/import-time-cleanup" branch.

**P3-1 — Lazy `runtime.__init__` and `runtime.loop.__init__`:**
- Cheap leaf modules stay eager (callbacks, config, llm_client, llm_types, tools, stats — zero transitive engine cost).
- `AgentLoop` (and engine.py beneath it) loads via PEP-562 `__getattr__` on first access. Cached on module dict so the lazy path runs at most once per name.
- Phase submodules (phases, reflection, checkpoint, todos, verification, remediation, tracing) also lazy now — the eager `from . import phases, ...` line in loop/__init__ was unnecessary.
- Both `from jyagent.runtime import AgentLoop` and `jyagent.runtime.AgentLoop` work — Python's `from X import Y` falls through to `__getattr__` automatically.

**P3-2 — Lazy executor:**
- Removed `tool_dispatch_executor = get_tool_dispatch_executor(8)` at module bottom. The helper is already lazy-grow; `AgentLoop.__init__` is the canonical caller. `atexit.register(...)` only fires inside the helper now.
- One test consumer was `test_parallel_batch_larger_than_dispatch_pool_completes` which `from engine import _tool_dispatch_executor` snapshotted at module load. Switched to live attr access (`le._tool_dispatch_executor`) + a `_get_tool_dispatch_executor(8)` to trigger lazy init before the read.

**Measured impact** (fresh interpreter):
- import time: 59.0 ms → 16.9 ms (-71%)
- jyagent modules loaded: 33 → 12
- engine.py loaded: True → False
- atexit handlers: 2 → 0
- pool initialized: yes (cap=8) → No (cap=0)

**Codex review caught a real P2 regression on the first commit:**
> The fallback `pool = executor or tool_dispatch_executor` receives None for callers that pass `executor=None`. Worked before because the global was eagerly set; needs lazy-init via `get_tool_dispatch_executor(max_workers)`.

Production was fine (AgentLoop always passes `loop._executor`), but direct callers of `tool_executor.execute_tools(executor=None)` and the back-compat `engine._execute_tools()` shim would AttributeError on parallel batches. Fix: replaced the module-global fallback with a lazy `get_tool_dispatch_executor(max_workers)` call. Added regression test in subprocess (so pool guaranteed None at start) that submits a 2-tool parallel batch with `executor=None`, asserts both succeed, and verifies the pool was lazy-materialised.

**Tests:** 615 → 623 (+8 in TestC4ImportTimeCleanup, all subprocess-isolated so pytest's hot module cache doesn't mask laziness). 0 regressions. One flaky concurrency probe in test_memory_tiers (`test_append_journal_no_duplicate_header_under_concurrency`) failed once during the full suite under load but passed 5/5 in isolation — pre-existing, unrelated.

**Process notes:**
- Codex review came back clean and fast this time (172s, single P2). No provider flapping today.
- "Codex as second opinion" rule paid off again: I'd shipped without the lazy-init fallback fix; Codex's audit caught the direct-caller path before merge.
- Honest correction at the start (C1 done, not open) was important — I'd misread the topic file name `runtime-c1-c4-deferrals.md` and almost wasted the user's time on already-shipped work.

**Branches:** runtime/import-time-cleanup deleted after safe merge. /tmp/import_cleanup_commit_msg.txt and /tmp/p3_followup_msg.txt left for traceability.

**Remaining open Codex items** (Part 1/2/3, lower priority):
- P1-11: ToolRegistry live-read accessors → @deprecated (~1h)
- P2-1: phase policy collapse for max_steps≤2 (~1-2h)
- P2-10: max-step fallback tool_choice="none" provider-dependence (~2h)
- P1-7/8: ToolResult string-content typing (cosmetic ~30min)
- obs-8: skills.py 28KB single file split (~half day)
- C5b: 6 remaining uncovered step.py paths (~2h, low ROI per yesterday)

## 2026-04-27 18:13 [infra]
Set up SSH host `clawdev` (14.103.184.220, root, Tencent Cloud Beijing VM, 8vCPU/30G/100G, Ubuntu 22.04). Rotated leaked root password (was = hostname). Installed Docker CE 29.4.1 via Aliyun mirror with daocloud/tencent registry mirrors. Installed K8s 1.34.7 single-node kubeadm cluster: containerd 2.2.3 CRI (had to fix sandbox key name and config_path), Aliyun image-repo for control plane, Flannel CNI v0.28.4 (rewrote ghcr.io→ghcr.m.daocloud.io). All pods Ready, smoke test passed. Kubeconfig at /root/.kube/config; /etc/profile.d/kube.sh sets KUBECONFIG, completion, k alias. Join token expires in 24h: `kubeadm join 10.159.3.104:6443 --token 8i9mpl.j7tj6noy0o5dhg17 --discovery-token-ca-cert-hash sha256:33a964837c300ef82d28af70f30e2ccd1e81ce41ad4beb078a16c0fd4df373ca` — regenerate with `kubeadm token create --print-join-command` if needed.

## 2026-04-27 18:28 [ship]
2026-04-27 18:30 — P1-11 ToolRegistry live-read deprecation shipped (commits 493a34d, 2a677b0, merged 353ec80, pushed)

Followed P3-1+P3-2 with the next item from the user's morning menu. Marked 6 footgun methods on `ToolRegistry` as @deprecated (PEP 702) so external callers see a runtime warning + IDE strikethrough but back-compat is preserved.

**Audit before code:**
- Filtered the noisy grep (data/traces/* JSONL files inflate matches) to find real callers.
- Production: 2 callers (`agent.py:469` and `subagent.py:886`, both `snapshot()`).
- Tests: 1 caller in `test_compaction_upgrade.py` using `get_compaction_priority()`.
- 4 of the 6 deprecated methods (`is_parallel_safe`, `is_mutating`, `get_timeout_hint`, `get_large_input_keys`) had ZERO real callers — pure legacy footguns.

**Deprecated:** snapshot, is_parallel_safe, is_mutating, get_timeout_hint, get_large_input_keys, get_compaction_priority. **Kept undeprecated:** get_function, get_schema, get_schemas, get_functions, list_tools, version (legitimate single-lookup, batch-atomicity not relevant).

**Migration target:** `registry.freeze()` once, then read from the resulting `ToolBatch`. Migrated all 3 in-tree callers to the new pattern.

**Tests added** (9 in TestC4ToolRegistryLiveReadDeprecation): 6 verify each method emits DeprecationWarning AND returns the correct value (back-compat); 1 verifies undeprecated lookups DON'T warn (warnings-as-errors); 1 verifies the freeze() migration target is warning-free; 1 static-grep test that scans the entire `jyagent/` package for `registry.<deprecated>(` patterns to catch accidental re-introduction. 623→632, 0 regressions.

**Codex review caught one P1:**
> `warnings.deprecated` was added in Python 3.13. `pyproject.toml` requires Python >=3.12, so importing `jyagent.runtime.tools.registry` on 3.12 raises AttributeError at class-def time and breaks the entire package.

I'd missed this completely — `warnings.deprecated` works fine on my 3.14 venv. The `requires-python = ">=3.12"` line in pyproject.toml was the giveaway. Fix:
- Switch to `typing_extensions.deprecated` (PEP 702 backport).
- Same API, works on 3.12+, forwards to stdlib on 3.13+.
- Added `typing-extensions>=4.5` as explicit dep (was transitive only).

Verified manually with a fresh 3.12.10 venv (`uv python install 3.12`): import works, `@deprecated`-decorated method emits `DeprecationWarning` with the expected message.

**Codex test-runner artefact noted:** Codex's `pytest -q` run from its own sandbox reported "DID NOT WARN" failures on the deprecation tests. My local run (worktree `.venv`, Python 3.14.3) passed all 9. The behavioural difference is likely a pytest plugin / warning-filter delta in Codex's environment, not a real bug. The P1 about 3.12 compat was the real finding worth acting on; the test-failure noise was probably how Codex *first* surfaced "something's wrong with the deprecation" before tracing it back to the version requirement.

**Process notes:**
- Audit-first paid off: knowing exactly which 3 call sites existed turned a "scary public API change" into a 5-line migration.
- The static-grep regression test (`test_no_in_tree_callers_remain`) is cheap insurance against future refactors silently re-introducing the footgun pattern.
- Codex review is now 2-for-2 on catching real issues since I started routing through it (P3-2 lazy-init regression on 0c9f77b; P1-11 3.12 compat on 493a34d). The "second opinion" rule is paying off systematically.

**Branch hygiene:** runtime/p1-11-deprecate-live-reads deleted after safe merge.

**Remaining open from the 2026-04-25 review:**
- (User explicitly said: ignore the rest after P1-11.)
- For reference, lower-priority items still on the bench: P2-1 (phase policy max_steps≤2), P2-10 (tool_choice provider-dependence), P1-7/8 (ToolResult typing cosmetic), obs-8 (skills.py 28 KB split), C5b (step.py coverage gaps).

